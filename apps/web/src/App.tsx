// App.tsx — placeholder shell. Login / lobby / fixed-stage room land in M1+.

export function App() {
  return (
    <div style={{ fontFamily: 'sans-serif', padding: 24 }}>
      <h1>FreeKill Web</h1>
      <p>骨架占位。客户端逻辑层 = wasmoon 托管原版 Lua;此处仅渲染 notifyUI 增量。</p>
      <p>下一步:M0 网关连通 + 登录/大厅(见 ../../分析/PROGRESS.md)。</p>
    </div>
  )
}
