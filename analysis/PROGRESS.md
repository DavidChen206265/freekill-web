# PROGRESS(人工/AI 维护 · 进度与决策)

> 本文件由 AI 在工作中更新(经 `/sync`)。PROJECT_STATE.md 是自动生成的事实快照,二者互补。

## 当前阶段

**M2 ✅ → M3 ✅(路由+重连/旁观)→ M4 ✅(切片 I 交互补全 + 切片 V 视觉动画/音频,均真机验收通过)→ 已部署 VPS。下一步:M5(扩展包 UI 兼容 + i18n 全量 + MD5 + Qt↔Web 混连),范围待用户拍板。**

基础身份局浏览器完整跑通,核心架构全实证,M3/M4 全部真机验收通过,已部署 VPS(`docker compose`,Caddy HTTPS/WSS,仅 freekill-core)。M4 切片 V(视觉动画+音频)经多轮验收反馈迭代修复后完成——含卡牌飞行(用牌/摸牌/弃牌/装备入槽/对手手牌)、Emotion/技能发动精灵、受击抖动、目标指示线(箭头+红环脉冲)、音频、Toast、桌面牌注脚、五谷提示;并修了一个关键的 **React StrictMode `oncancel` 误删效果** bug(指示线/装备飞行/红环全不可见的真因)和 **`.dockerignore` 漏 audio/anim 目录**导致服务器无声/无精灵的部署 bug。详见变更日志。

**M4 切片 V 完成记录(2026-06-09,已合 master 推送,提交 fe260d0…5524f8a)**:V-0 总闸+animationStore / V-1 Indicate 连线 / V-2 Emotion 精灵 / V-3 tremble+濒死/阵亡 / V-4 技能发动框 / V-5 音频(原生 Audio,非 Howler)/ V-6 Toast+桌面牌注脚;逐行审计修 5 处还原错误;验收反馈修复:技能横幅淡出、桌面结算区参考系归位+居中、即时结算牌飞行(Destroy 改延后 vanish 1.5s 照搬 TablePile.qml vanishTimer)、Processing→Discard 保位(照搬 moveCards SKIP 守卫)、摸牌动画(lastMoved 累积防批处理丢失)、photo 动画居中、装备/对手手牌飞入、指示线箭头+红环、StrictMode oncancel bug、部署 audio/anim 缺失。**方法论铁律(已沉淀 memory)**:别拿单测当真实验证——先 probe 真实 VM/CBOR 数据契约再下结论(本切片多次靠 ag-probe/indicate-probe 定位真因,推翻基于假设的误诊)。

**通信日志系统可用于后续 debug**:`localStorage.fk_log='debug'`(web)/ `FK_LOG=debug`(gateway)开全量结构化日志;VmDebugPanel 可导出 JSON;notifyUI 未消费命令探测器(`notifyCommands.ts`)防五谷类回归。

## 待办(下一步)

**M4 ✅ 全部完成并真机验收通过。M5 进行中**(用户拍板:先 M5-a 单局完整度,因近期加扩展包 → M5-b/c 紧随):

- **M5-a 补渲染表(单局体验)** — **M5-a-1 MiscStatus ✅** + **M5-a-2 标记区完整化 ✅**(提交 a69f544,待真机验收):回合/计时/牌堆数 + `@!`图片标记/`@@`隐值/`@`文本(VM 桥按前缀分类,对真 VM 验证)。**M5-a-3 LimitSkillArea + M5-a-4 banner 延后**与 M5-b 一起(限定技/banner 主要在扩展包出现)。
- **M5-c MD5 manifest ✅(提交 0f897c8)**:`computeFlistMd5` 字节级复现 asio calcFileMD5(util.cpp),**对真 asio 双验证**(core-only e48d6db7 / +utility+standard_ex+sp 三包 8efa2cc 均字节一致)。buildManifest + compute-md5.mjs CLI + 单测。
- **M5-b QmlMark + 加载扩展包(进行中)**:
  - **阶段A-1 QmlMark `@[type]` 文本标记 ✅(cf29909)**:`__fkReadPlayers` 加 `@[` 分支调 `GetQmlMark` 取 `how_to_show` 文本,对真 VM 验证(零 qml 依赖、覆盖最广)。
  - **加载扩展包 utility+standard_ex+sp ✅(acf8072,全链路实测)**:asio 拷三包 + packages.db 启用 sp(原 enabled=0);web sync 新增 EXTENSION_PACKS + mount.ts `manifest.extra`(各包 lua 挂 VFS),美术/anim/audio 走 ART_PACKS 懒加载;`.dockerignore` 放行三包全树 + dockerignore.repo-root。**握手 MD5 实测**:gateway 用 8efa2cc 跑 live handshake.test → NetworkDelayTest→Setup→lobby 全过(asio 接受);反证 core-only e48d6db7 → 拒,证明 MD5 门真实有效且算值正确。sync 实测 utility 23/standard_ex 51/sp 114 代码文件,audio 增至 602;vite 服务三包 lua(200)。
  - **阶段A-2 ChooseSkillBox ✅(8f8ade7)**:扫 sp/standard_ex 唯一用到的 CustomDialog qml_path = `utility/qml/ChooseSkillBox.qml`(sp 萧德选技能)。popupStore CustomDialog 按 qml_path 派发 → 新 `chooseSkill` 弹窗(多选技能 min..max,复用 choices 同构),reply 选中技能名数组;reply 契约读源码核实(receive_decode=false 但 CBOR 传输层解码,result[1] 与发数组一致)。其余 qml_path/MiniGame 仍走 unsupported 兜底。
  - **下一步**:按需移植更多 utility 共享框(ViewPile/PlayerBox 点击查看型等)+ 带 qml 的点击查看型 QmlMark;真机选 sp/界限突破武将开局 + 萧德选技能验收。
- **M5-d i18n 全量**:接 VM `Fk:translate` 替换硬编码 `zh.ts`(保留 lobby fallback)。可与 M5-b 并行。
- **M5-e Qt↔Web 混连验收**:依赖 M5-c。
- **(可选)R-PERF/R-VM 真浏览器内存复测**;**(延后)回放/录像/战绩、大厅设置/建房丰富度/资料库**(saveRecord 空桩,记录在案不阻塞核心)。

**用户拍板(2026-06-09)的下一步顺序**:先 **M5-a 单局完整度补渲染**(详见 `分析/M5-a_detailed_plan.md`:MiscStatus 回合/计时/牌堆数 → 标记区图标/特殊前缀 → LimitSkillArea → banner),因纯增量、无外部依赖、最快见效;**之后因近期要加扩展包(sp/tenyear/ol 等)→ M5-b(QmlMark+utility 共享框)+ M5-c(MD5 manifest,因包集合变更握手 MD5 会变)优先级随之提高**。M5-d(i18n)可与之并行。M5-e 混连依赖 M5-c。

## 决策记录

- **下一步开发顺序(2026-06-09 用户拍板)**:M4 全完成 → **M5-a 单局完整度优先**(无依赖、最快见效)→ 因近期加扩展包,**M5-b 扩展 UI + M5-c MD5** 紧随(加包后 MD5 握手会变,需 manifest 替代硬编码)。M5-a 详规 `分析/M5-a_detailed_plan.md`。
- 客户端逻辑层 = WASM 托管原版 Lua(见 freekill_web_implementation_plan.md §4)。
- 自动化工作流:SessionStart 钩子自动重建 PROJECT_STATE.md 并注入上下文;`/sync` 命令在收尾时更新 PROGRESS.md/计划/风险。事实层(脚本生成)与判断层(AI 维护)分离。
- **R-PERF 度量法**:每场景独立子进程 + `--expose-gc`,挂载后/启动后各 GC 再读 RSS,delta 即引擎成本。三场景:base(4 包)/ selective(base+utility,sp,tenyear,ol)/ full(全量)。代码 `freekill-web-spike/src/perf_{spike,run}.mjs`,`npm run perf`,结果 `perf-result.json`。
- **R-VM 度量法**:回放 `captured-packets.json`(server VM 自打一局的真实 packet 流,player-1 视角 1357 包/803 notifyUI)N 局,每局后 GC 再采样内存。两模式对比:`fresh`(每局 new LuaFactory,全新 WASM 实例)vs `reuse`(单 factory 复用,每局 createEngine+close)。代码 `freekill-web-spike/src/vm_{spike,run}.mjs`,`npm run vm`,结果 `vm-result.json`。**判定以 `process.memoryUsage().external`(ArrayBuffer/WASM 线性堆的真实归属)为准,非 RSS**——Node/glibc 保留已释放页,RSS 因碎片爬升不代表泄漏。
- **选择性加载杠杆确认**:`ModManager:loadPackages` 用 `FileIO.ls("packages")` 自动发现含 init.lua 的目录;Web 端"只加载该局所需包"= 只向 VFS 挂载所需包目录(未挂载者不会被发现),无需改引擎,与 disabled_packs 反向白名单一致。

## 变更日志

- 2026-06-10 **项目自包含化:analysis/audit 入 git + 复刻扩展包机制(packages-upstream)**。用户两项要求,严格走工作流(读状态→规划→AskUserQuestion 定关键决策→执行→逐步自验→更新计划)。**①目录重组**:`分析/`→`freekill-web/analysis/`、`audit/`→`freekill-web/audit/`,都移进 freekill-web 并 git 追踪(此前 分析/ 在仓库外不受版本控制)。同步更新基础设施:`.claude/scripts/project-state.mjs`(STATE/PROGRESS 路径 + TRACKED 去掉独立"分析"、排除 packages-upstream + stdout footer)、`.claude/skills/project-sync/SKILL.md`、`CLAUDE.md`(文档映射全部 `分析/`→`freekill-web/analysis/`)、两份 plan 内 `分析/` 自引用→相对 `analysis/`。SessionStart 钩子实测仍正常重建状态。**②扩展包机制**(复刻 FreeKill 原版):全部 32 个包(freekill-core+基础+27 扩展,~1.5GB)镜像进 `freekill-web/packages-upstream/`(复刻 `FreeKill-release/packages/` 结构)。**gitignore 策略**:忽略所有包内容,仅追踪每包一个 `.gitkeep`(32 个)+ README.md——**结构入库、内容不入库**,便于未来从 FreeKill 移植/扩展新包。踩坑:upstream 包自带嵌套 `.git`(28 个,被 git 当 submodule)+ 自带 `.gitignore` 覆盖我的规则,已清除(`find -name .git -delete` + 删 stray .gitignore),最终精确追踪 33 文件(README+32 gitkeep),内容确认 ignore。**sync 重指向**:`sync-fk-assets.mjs` 优先从 packages-upstream 取包,缺失回退仓库外 FreeKill-release(`FK_PACKAGES_DIR` 可覆盖);lua-native 3 个测试的 CORE 路径同样优先 mirror 带回退。**Docker**:`.dockerignore` + `dockerignore.repo-root` 排除 packages-upstream(镜像构建走 FreeKill-release 回退,避免 1.5GB 进镜像)+ 修 `分析/audit`→`freekill-web/analysis|audit`。**自验全绿**:sync 从 mirror 跑出与原一致(utility23/standard_ex51/sp114、audio602);FK_PACKAGES_DIR 回退路径实测可用;typecheck 全绿;test 全过(protocol20/lua-native16/web120);build 绿。README + 实现计划 §6 目录结构 + §7 资源方案 + M3-M6 bring-up 均记录新机制与启用新包四步流程。

- 2026-06-10 **分析/ 规划全面更新 + 删除冗余计划(纯文档,未改码)**。基于当前状况(M2/M3/M4 + M5-a/b/c + PWA 均已完成)与刚完成的审计重审,对规划做对齐 + 减负。**删除 4 份冗余文档**(分析/ 非 git,删除不可逆,用户明确授权):①`PWA_plan.md`(PWA 已落地,沉淀于变更日志);②`M5-a_detailed_plan.md`(M5-a-1/a-2 已完成,a-3/a-4 折入 M5 计划剩余项);③`CUSTOMDIALOG_MINIGAME_方案.md`(QmlMark/CustomDialog/MiniGame 分级结论已折入 M3-M6 计划 M5 节内联);④`AUDIT_REPORTS_META_REVIEW.md`(针对旧审计报告的复核,已被 2026-06-10 重审 + 主控逐条纠错取代)。**更新 2 份留存计划**:`M3-M6_detailed_plan.md` 重写为「M5-M6 聚焦」——M3/M4 收为文末历史指针(详情在变更日志),M5 标注 a/b/c 已完成 + 剩余项(更多 utility 框/QmlMark 点击型/i18n/混连),内联折入扩展框分级 + 审计真缺口清单(含 ART_PKGS P7-032 bug);`freekill_web_implementation_plan.md` §9 里程碑 M3/M4/M5-c 标完成、§10 验收 M3 标无感重连已实现、§11 风险登记册 R-ROUTE/R-MD5/R-DND 降为低/已消解、A5 标 M5-c 落地、清除所有指向已删 META_REVIEW 的引用。**当前 分析/ 仅留 4 个 .md**:实现计划、M3-M6(实为 M5-M6)计划、PROGRESS、PROJECT_STATE(自动生成)。CLAUDE.md 文档映射只引稳定文件,无悬挂引用。**下一步重心**:M5 剩余(按导入包驱动的 utility 共享框 / QmlMark 点击型 / i18n / 混连)或先修 ART_PKGS 小 bug,待用户定。

- 2026-06-10 **audit/ 12 份审计报告全面重新审计 + 补全(纯文档,未改码)**。用户要求:严格走工作流,逐行读 FreeKill 源码与本项目代码重新审计,更新 `audit/phase*.md` 全部 12 份报告并补全漏掉的 UI 元素。原报告写于 M2 期(~06-06),严重过时(未反映 M3 路由/重连/旁观、M4-I 交互补全、M4-V 视觉动画/音频、M5 MiscStatus/标记区/QmlMark/扩展包/MD5)。**方法**:沿用项目「并行只读 audit agent + 主控复核」范式,分两批 12 个 Explore agent(批A 协议/逻辑/启动:phase1/2/2b/2c/6/8;批B UI/资源:phase2d/3/4/5/7/9),各逐行核实对应 source+web、刷新 status、补新行;主控对高风险结论**逐条核实源码**纠错。**主控纠错(关键)**:phase5 agent 误报 P5-023/024(AskForCardsAndChoice/MoveCardInBoard)为 stub、P5-025(Interaction 控件)为 not restored——实测 RequestPopup.tsx:263/320 是完整组件、Dashboard.tsx:108 InteractionPanel 渲染 combo/spin/checkbox/cardname,已纠为 successfully restored;phase7 agent 误报 P7-009 LogEvent 音频为 stub——实测 vmStore.ts:163-188 handleLogEvent 完整接 playSystem/playSkillSound/playDeath,已纠正;phase2c agent 虚构不存在的 `RequestUI/*.tsx` 目录——已改回 RequestPopup.tsx+popupStore.ts。**真实新发现 bug**(唯一):`skin.ts:13`/`audio.ts:16` 的 `ART_PKGS=[standard,standard_cards,maneuvering]` 与 `sync-fk-assets.mjs:32` 的 `ART_PACKS`(含 utility/standard_ex/sp)不一致——扩展包美术/标记图的候选回退扫描漏掉(直接路径仍可解析),记入 phase7 P7-006/P7-032(低危,待修)。**状态分布合理**:协议/notifyUI/牌桌/lua(phase2/2d/5/6)restored 大幅上升(M3/M4/M5),大厅/目录/回放(phase3/8/9)仍多为 not restored(确实未实现,候选 M6 百科/回放里程碑)。**翻译版 `audit/translated/*.zh.md` 本轮未同步**(已在各报告头标注 stale)。仍存真缺口供后续取用:DetailedChoice/CheckBox、手牌禁用原因文字、标记点击开牌堆(`@&`/`@$`/`@[...]`)、HandcardViewer、完整武将详情/总览页、回放/录像、大厅设置/资料库、quit 二次确认(P4-013)、AddRobot 未读 serverFeatures(P4-004)、ART_PKGS 不一致(P7-032)。

- 2026-06-09 **计划更新 + 冗余清理 + 下一步定为 M5-a(纯文档,未改码)**。M4(切片 I 交互补全 + 切片 V 视觉动画/音频)全部完成并真机验收通过(切片 V 经多轮验收反馈迭代:卡牌飞行全覆盖、指示线箭头+红环、StrictMode oncancel bug、部署 audio/anim 缺失等,均已合 master 推送至 5524f8a)。据此:①**更新当前阶段**为「M2/M3/M4 ✅ → 下一步 M5」;②**删除两份已完成切片的详规**(`M4-V_detailed_plan.md`、`AUDIT_M4_I.md`——切片完成后其内容已沉淀于本变更日志,沿用既有「完成即删详规」惯例,如此前删 M2_detailed_plan/AUDIT_6.5;并修掉实现计划/M3-M6 计划里指向它们的引用,避免悬挂);③**用户拍板下一步**:先 **M5-a 单局完整度补渲染**(纯增量、无依赖、最快见效),因近期要加扩展包(sp/tenyear/ol),**M5-b 扩展 UI + M5-c MD5 manifest 紧随**(加包后握手 MD5 会变需 manifest 替代硬编码)。④**新建 `分析/M5-a_detailed_plan.md`**:逐行核实 `MiscStatus.qml`/`PicMarkArea.qml`/`MarkArea.qml` + RoomLogic.js 回调,拆 M5-a-1 MiscStatus(回合/计时/牌堆数,最简先做)→ M5-a-2 标记区完整化(`@!`图标/`@@`隐值/`@!!`描述,扩展 readPlayers VM 桥)→ M5-a-3 LimitSkillArea → M5-a-4 banner(可选)。当前 `分析/` 仅留:实现计划、M3-M6 计划、M5-a 详规、CUSTOMDIALOG_MINIGAME 方案(喂 M5-b)、AUDIT_REPORTS_META_REVIEW(M5 审计基线)、PROGRESS、PROJECT_STATE。

- 2026-06-09 **M4 切片 V 视觉动画+音频代码完成**(freekill-web 提交 fe260d0…5524f8a,已合 master)。先写 probe 喂真 VM 确认 `Animate`/`LogEvent` 数据契约再实现。V-0 总闸+animationStore / V-1 Indicate 连线(箭头+红环脉冲)/ V-2 Emotion 精灵(anim.json 帧清单,懒加载)/ V-3 tremble+濒死/阵亡(无自创飘字)/ V-4 技能发动框 / V-5 音频(原生 Audio 替代 Howler)/ V-6 Toast+桌面牌注脚。逐行审计修 5 处还原错误。**验收反馈多轮修复**:技能横幅淡出、桌面结算区参考系归位(roomArea 高度 + 水平居中)、即时结算牌飞行(Destroy 改延后 vanish 1.5s 照搬 TablePile.qml vanishTimer)、Processing→Discard 保位(照搬 moveCards SKIP 守卫)、摸牌动画(lastMoved 累积防 React 批处理丢失)、photo 动画居中、装备/对手手牌飞入槽位、**React StrictMode `oncancel` 误删效果**(指示线/装备飞行/红环全不可见的真因)、**`.dockerignore` 漏 audio/anim 目录**致服务器无声无精灵的部署 bug。收尾:已实现命令移入 HANDLED_EXPLICIT,探测器+覆盖闸收紧。自验全绿(web 115/lua-native 14/shared 9/protocol 20);多次靠 ag-probe/indicate-probe 定位真因、推翻假设性误诊。

- 2026-06-09 **PWA 化 + /fk 资源 SW 本地缓存 + 两个交互 bug 修复**(freekill-web,未推 master,待真机)。**PWA**(917ad48):用户反馈 /fk 资源(53MB/2183 文件)偶发加载失败致缺语音/动画 → vite-plugin-pwa(Workbox generateSW),运行时 CacheFirst 缓存 `/fk/**`(cacheName fk-assets,不预下 53MB、miss 回源、cacheableResponse[0,200]+rangeRequests 兼容音频 206),app 壳预缓存仅 478KB(globIgnores fk);完整可安装 manifest(官方 icon.png→192/512/maskable+favicon);devOptions.enabled:false 开发不启 SW 防污染 HMR;Caddyfile sw.js/manifest 加 no-cache 保 autoUpdate。实测 vite preview 产物:sw.js(text/javascript)/manifest/fk 均 200,precache 无 fk 路径。**交互 bug 修复**:① 弹窗框可拖动/折叠+不再全屏遮罩(ef39697)——核实 QML GraphicsBox 本就浮动+DragHandler 无遮罩,web 共享 Modal 误用 inset:0 全屏遮罩挡住并发交互(同 AG/无懈根因),重写 DraggableBox(浮动/无遮罩/标题拖动/➖➕折叠),AG 统一;② 回房间清残留(同提交)——backToRoom 漏清 9 个瞬态 store,新增 vmStore.resetForNewGame()+gameStore.backToRoom 剥离每玩家对局态仅留身份。③ 之前(af43780)五谷选牌框遮无懈修复。自验:typecheck/build 全 workspace 绿,web 120 测试。

- 2026-06-09 **M5 推进:M5-a 单局完整度 + M5-c MD5 算法 + M5-b 加载扩展包(全链路实测打通)**。严格走工作流(读源码→实现→**对真 VM/asio 验证**→自验)。**M5-a-1/2**(a69f544):MiscStatus 回合/计时/牌堆数(对照 MiscStatus.qml,消费 UpdateDrawPile/RoundNum)+ 标记区完整化(`__fkReadPlayers` 按前缀分类 `@!`图片/`@@`隐值/`@`文本,对真 VM 断言三类正确)。**M5-c**(0f897c8):`computeFlistMd5` 逐行复现 asio `calcFileMD5`(util.cpp:CRLF→LF/字母序/lua→qml→js 分组/内置+.disabled+disabled 排除/`packages/`路径键),**对真 asio 跑实际 packages 得 e48d6db7(core)字节一致**;+buildManifest+compute-md5.mjs CLI+单测。**M5-b-A1**(cf29909):QmlMark `@[type]` 文本标记——桥加 `@[` 分支调 GetQmlMark 取 how_to_show 文本(对真 VM 验证 "MARK:5")。**M5-b 加载扩展包**(acf8072,**全链路实测**):asio 拷 utility+standard_ex+sp、packages.db 启用 sp(原 0);web sync 加 EXTENSION_PACKS、mount.ts 支持 `manifest.extra`(各包 lua 挂 /fk/packages/<base>)、美术/anim/audio 走 ART_PACKS 懒加载;`.dockerignore` 放行三包全树。**实测**:asio 自算 MD5=8efa2cc 与我 computeFlistMd5 字节一致;gateway 用 8efa2cc 跑 live handshake→NetworkDelayTest→Setup→lobby 全过(asio 接受),**反证 core-only e48d6db7→拒**(证明 MD5 门真实有效+算值正确);vite 服务三包 lua 200。自验:typecheck/build 全绿,web 118/lua-native 16/assets 3/gateway live handshake PASS。遗留:浏览器真机选 sp/界限突破武将开局眼见;VPS 部署需同步装包+重算 FK_MD5(已记 docker-compose 两个验证值)。方法论铁律延续:先 probe 真实 VM/asio 再下结论(MD5 算法、QmlMark、mark 分类全对真后端验证,非仅单测)。**未推 master**(本地实测通过,待真机眼见后合)。

- 2026-06-09 **浏览器真机验收通过 + M4 切片 V(视觉动画+音频)详细规划定稿**(纯文档/规划,未改生产码)。用户确认浏览器验收:五谷选牌框弹出可点、刷新战报美化、重连/旁观/退房确认全部眼见无误 → M3 + M4 切片 I + 验收期三 bug 修复**全部真机通过**。据此严格走工作流(读状态→源码核实→规划)为切片 V 制定 `分析/M4-V_detailed_plan.md`:**逐条核实** RoomLogic.js 动画回调(`Animate` 1310-1372 / `LogEvent` 1374-1442 / `doIndicate` 568 / `setEmotion` 411)、QML 组件精确参数(IndicatorLine 700ms 三段/PixmapAnimation 50ms·帧 scale0.75/SkillInvokeAnimation 1640ms 四段/Photo tremble ±15px 200ms/death 灰化 300ms)、room.lua 发包路径(doAnimate/sendLogEvent/sendFootnote)、SkinBank getAudio 解析顺序、审计条目(P2D-020/028、P7-008/009/010/012、P7-027 toast)。**关键核实结论**:①AG/视觉命令是纯瞬时、不进 VM 镜像,须显式消费但失败只降级观感(非五谷类状态 bug);②**QML 无独立伤害飘字组件**(靠 emotion+tremble+音效)——规划明令不自创飘字;③LightBox 源码本就空实现→no-op;④SuperLightBox 只支持内置默认、包专属复杂 qml 归 M5。**切片拆分** V-0 总闸+animationStore(前置)→V-1 Indicate 连线→V-2 Emotion 精灵(+anim 资源 sync 懒加载)→V-3 tremble/濒死/阵亡灰化→V-4 技能发动框→V-5 音频(Howler+audio sync+自动播放 unlock)→V-6 Toast+桌面牌注脚→收尾(已实现命令从 notifyCommands.ts KNOWN_DEFERRED 移入 HANDLED_EXPLICIT,探测器收紧)。复用 R-ANIM(CardLayer WAAPI 场景坐标)与 focusStore/PhotoFocusBar(瞬时 store + per-Photo 组件)两个已落地范式。风险:资源体量(anim 257+包数百帧/audio 242 mp3)全懒加载、不进 git/VM mount;浏览器自动播放需 unlock;帧数靠 sync 期生成清单(无 Backend.ls)。同步更新实现计划 §M4(切片 I✅/V 为下一步)、§11 风险(R-DND 降级已实现、新增 R-OBSERV 已消解)、删除已完成的 `LOGGING_FIDELITY_plan.md`、清理 8 个已合并本地 git 分支。用户确认浏览器验收:五谷选牌框弹出可点、刷新战报美化、重连/旁观/退房确认全部眼见无误 → M3 + M4 切片 I + 验收期三 bug 修复**全部真机通过**。据此严格走工作流(读状态→源码核实→规划)为切片 V 制定 `分析/M4-V_detailed_plan.md`:**逐条核实** RoomLogic.js 动画回调(`Animate` 1310-1372 / `LogEvent` 1374-1442 / `doIndicate` 568 / `setEmotion` 411)、QML 组件精确参数(IndicatorLine 700ms 三段/PixmapAnimation 50ms·帧 scale0.75/SkillInvokeAnimation 1640ms 四段/Photo tremble ±15px 200ms/death 灰化 300ms)、room.lua 发包路径(doAnimate/sendLogEvent/sendFootnote)、SkinBank getAudio 解析顺序、审计条目(P2D-020/028、P7-008/009/010/012、P7-027 toast)。**关键核实结论**:①AG/视觉命令是纯瞬时、不进 VM 镜像,须显式消费但失败只降级观感(非五谷类状态 bug);②**QML 无独立伤害飘字组件**(靠 emotion+tremble+音效)——规划明令不自创飘字;③LightBox 源码本就空实现→no-op;④SuperLightBox 只支持内置默认、包专属复杂 qml 归 M5。**切片拆分** V-0 总闸+animationStore(前置)→V-1 Indicate 连线→V-2 Emotion 精灵(+anim 资源 sync 懒加载)→V-3 tremble/濒死/阵亡灰化→V-4 技能发动框→V-5 音频(Howler+audio sync+自动播放 unlock)→V-6 Toast+桌面牌注脚→收尾(已实现命令从 notifyCommands.ts KNOWN_DEFERRED 移入 HANDLED_EXPLICIT,探测器收紧)。复用 R-ANIM(CardLayer WAAPI 场景坐标)与 focusStore/PhotoFocusBar(瞬时 store + per-Photo 组件)两个已落地范式。风险:资源体量(anim 257+包数百帧/audio 242 mp3)全懒加载、不进 git/VM mount;浏览器自动播放需 unlock;帧数靠 sync 期生成清单(无 Backend.ls)。同步更新实现计划 §M4(切片 I✅/V 为下一步)、§11 风险(R-DND 降级已实现、新增 R-OBSERV 已消解)、删除已完成的 `LOGGING_FIDELITY_plan.md`、清理 8 个已合并本地 git 分支。

- 2026-06-09 **五谷AG框/战报美化「真修复」——上次误诊,本次用探针定位真根因**(freekill-web 提交 188f0f4,分支 fix-ag-realshow-and-log-cbor,已合 master 推送)。上次(3a0ff43)两个 bug 都只用单测验证了**假设**而非真实数据流,用户实测仍坏。本次先 probe 真实 VM/CBOR 再下结论(血泪教训:别拿单测当真实验证)。**#1 五谷AG框不显示**:写 ag-probe.mjs 喂真 VM 真实 FillAG/AskForAG CBOR,**证明 AG 命令间无 CancelRequest**(AG 在 client.lua 无 callback→走 else 直接 notifyUI,不触发 client.lua:48 双active守卫)——上次的 `clearExceptAg` 修了不存在的问题。真根因:截图显示 AskForAG 已触发(倒计时 69s 在跑=ACTIVATE_COMMANDS 命中)但无框,说明 AskForAG 到达时 `active` 不是 'ag'(FillAG 的框丢失/被替换),而旧 AskForAG 处理器仅在 active 已是 'ag' 时激活、否则静默 no-op→无框+计时器空跑(正是截图)。修:AskForAG 自带 id_list(room.lua:2738 data={id_list,cancelable,reason}),改为**无既有 AG 框时用自身 id_list 重建框**(别的 popup 占用时替换),不再依赖 FillAG 必先到——忠实(同批牌)且防御。**#2 战报replay原始JSON**:用 cbor-x 解码真实 GameLog 包发现 LogMessage 字段(type/arg/card/from/to)是 **CBOR 字节串**;网关旧 `JSON.stringify(env.data)` 把 Uint8Array 变成 `{"type":"Buffer",...}` 有损垃圾,parseMsg 无法解析→显原文。上次的 `__fkParseLog(json.decode)` 同样有损,根本错了。修:①网关缓冲 `env.raw`(base64 原始 CBOR)非有损 JSON;②`__fkParseLog` 改收原始 CBOR hex→`cbor.decode`(字节串→Lua串)→parseMsg,与实时 GameLog 路径(ClientCallback cbor.decode)完全一致;③routeEnvelope replay 把 base64→bytes 喂 `vm.parseLog`。**自验**:typecheck/build 绿,145 测试全绿;新增 agFlow.test(回放探针实测真实序列 5 例,含「无 FillAG 时 AskForAG 自建框」「别的 popup 被替换」健壮性回归);parseLog 测试改用 **CBOR 编码**(真字节串)断言 #Damage 美化出曹操/刘备 HTML(这版能抓上次漏掉的字节串 bug);真 asio refresh-test 跑通重连(无 VM 客户端故未到产 log 阶段,replay 内容浏览器验)。Docker 双构建链本地实测通过、frozen-lockfile 不变。**遗留**:浏览器真机眼见五谷框弹出可点 + 刷新战报美化待用户(VPS `git pull && docker compose up -d --build`)。

- 2026-06-09 **通信/执行结构化日志系统 + notifyUI 未消费命令探测器(五谷类隐患守卫)+ 两处保真修复**(freekill-web 提交 deb4b6f,分支 feat-logging-and-fidelity,基于 3a0ff43)。用户要求排查所有局内 UI 有无类似五谷的遗留问题 + 加详细通信/执行日志。**关键方法论结论(写入经验)**:派 2 个只读 audit agent 对照 QML,但复核发现**两份报告都因忽略本项目 VM 镜像架构(CLAUDE.md「数据真相源是 VM,不是增量」)严重高报**——`vmStore.feed` 每包后重读 `readPlayers/readSkills`,故 PropertyUpdate/ArrangeSeats/MaxCard/SetPlayerMark/UpdateSkill/Add|LoseSkill/UpdateHandcard 等都已由 VM 内部应用并镜像,**非 dropped**;且 EmptyRequest 并不在 ACTIVATE_COMMANDS(第二份报告头号「P0 计时器空转」系误读源码)。对真实 VM 发出的 41 个 notifyUI 命令逐一核实,**真正未消费的几乎全是视觉/动画类**(Animate/LogEvent/SetBanner/SetCardFootnote/ShowVirtualCard/UpdateLimitSkill/ChangeSkin…),已规划为 M4 切片 V,非意外 bug。五谷 bug 的特殊性在于它是**请求生命周期竞态**(CancelRequest 清弹窗),不是缺增量。→ **比一次性静态审计更可靠的是运行时探测器**:经验性标记任何「VM 发了没人接」的命令、并防未来回归。**实现**:①`packages/shared/src/logger.ts` 同构结构化 Logger(分类/级别/环形缓冲/注入式 sink/循环+BigInt 安全导出,9 单测);②web 接线(`diag/log.ts`,默认 silent,`localStorage.fk_log` 可调):net-in/out(gatewayClient)、vm-feed(feed)、vm-notify/reply(notifyUI sink);③**未消费命令探测器**(`diag/notifyCommands.ts` 纯分类器=活文档):`classifyNotify` 归类 explicit/popup/mirror/deferred/unhandled,unhandled=五谷类隐患 warn 一次+计数;VmDebugPanel 加「通信日志」区(级别切换/导出 JSON/清空 + 未消费命令红条);④gateway 接线(`log.ts`,`FK_LOG` env 门控):ws-bridge 双向包+生命周期(connect/park/handshake/log-replay/disconnect)、asio-client 握手阶段,不日志化密码;⑤**保真修复**:UpdateCard 未消费致卡面缓存陈旧(转化/翻面牌显旧图)——vmStore 加 UpdateCard 分支 readCards 重读覆盖 cardFaceStore(QML RoomLogic.js:680-705 同义);RequestPopup 按请求身份加 key 消除背靠背同类请求的 Modal 残留帧竞态。**自验**(自起 WSL asio+gateway):typecheck/build 全绿;shared 9/protocol 20/lua-native 14/web 97=140 测试全绿;**五谷类覆盖闸**(lua-native 回放真实整局 captured packet 流,断言每个 notifyUI 命令都被分类,**唯一未分类项 Animate 是 M4-V 视觉命令,无任何功能性五谷类缺口**——「无遗留」的经验证明);真 asio start-game e2e PASS,`FK_LOG` 门控有效(info 抑制 debug 包/debug 显示 net-in/out+握手全量);UpdateCard 对真 VM 断言 SetCardMark→readCards 反映新 mark。**不做(留 M4 切片 V)**:视觉/动画命令实现,探测器现以 deferred 归类不误报。**遗留**:浏览器真机眼见(开 fk_log=debug 看日志面板/导出)+ 五谷/卡转化保真待用户。

- 2026-06-09 **浏览器验收两 bug 修复:五谷丰登选牌框不显示 + 重连战报历史显示原始 JSON**(freekill-web 提交 3a0ff43,分支 fix-ag-box-and-log-replay)。先 `git pull` 更新 master(仅 docker 部署文件),再派 2 个只读 agent 定位 + 主控逐行读源码复验。**#1 五谷 AG 选牌框不显示**(真因经源码锁定):VM 在**每个** `AskFor*` 命令前都 `notifyUI("CancelRequest")`(client.lua:48-49 防双 active 权宜),恰落在 `FillAG`(铺牌)与激活牌堆的 `AskForAG` 之间;原 web CancelRequest 处理器 `popupStore.clear()`(vmStore.ts:209)把刚建好的 AG 框清掉,随后 `AskForAG` 只"修改"已有 ag 框却已为 null(popupStore.ts:217)→ 框永不显示。**照搬 QML**:CancelRequest 仅置 `state="notactive"`(RoomLogic.js:1221),AG 框在独立持久的 `manualBox`、只由 `CloseAG` 关闭(RoomLogic.js:1476)。修:新增 `popupStore.clearExceptAg`(保留 ag 框,CancelRequest 改用它);FillAG 置 `agInteractive=false`、AskForAG 置 `true`(镜像 manualBox.interactive RoomLogic.js:1462,铺牌期不可点);新增 `resolveAg`(出牌后回 cid 但保持框开+锁定直到 CloseAG,照搬 AG.qml:39-44 onClicked——之前用通用 resolve 会 `active:null` 过早关框,玩家看不到后续 TakeAG 标记)。**#2 重连后战报历史显示原始 JSON**:实时 GameLog 经 VM `parseMsg`→`notifyUI("GameLog", html)` 本地化成美化 HTML(clientbase.lua:564/lunarltk client.lua:230),而网关按 uuid 缓冲的是**原始 LogMessage JSON**(d06de1f,ws-bridge.ts:75),重连 `__gateway_log_replay` 时直接 `logStore.prepend` 原文(index.ts:259)**绕过 parseMsg**。修:新增 `__fkParseLog` VM 桥(`ClientInstance:parseMsg`)+ `ClientVm.parseLog`;replay 处理器改为把每条原始 JSON 经**重建后的 VM** parseMsg 美化(链到 feedChain 确保 VM 已 boot 且 Reconnect 状态镜像已落、parseMsg 能解析玩家名/座位),失败逐条回退原文。**自验**:typecheck/build 绿;web 92(popupStore +4:AG 跨 clearExceptAg 存活/非 AG 弹窗仍关闭/resolveAg 保持框开+回 cid)、lua-native 13(parseLog 对真 VM 断言 `#Damage` 美化出含 `<font>`+曹操/刘备本地化 HTML、非法输入回 ""、模板键已展开)、protocol 20 全绿。**遗留**:浏览器真机眼见(五谷多人轮选框可见可点、刷新重连战报历史美化)待用户。

- 2026-06-08 **验收续修:倒计时对齐服务端 + 重连恢复战报**(freekill-web 提交 d06de1f)。**#2 错过操作后进度条归零仍白等很久**:根因——客户端倒计时条硬编码 30s(timerStore），但建房 timeout 实为 90s,服务端按 `timestamp+timeout*1000+500` 计时(request.lua:210)→30s 条走完只做 UI 清理(不回复)→服务端还在数到 90s→白等约 60s。修:请求 envelope 带服务端真实 timeout+timestamp(ms epoch),routeEnvelope 捕获→`timerStore.setServerWindow`→activate() 用 `deadline=timestamp+timeout*1000` 对齐(时钟偏移 max 兜底),ui_emu+popup 都受益。**#1 刷新后战报不保留**:根因——asio room:serialize 不含历史日志(上游限制)。修(网关缓冲):GameLog 是服务器广播包(server.cpp:396),网关按 uuid 滚动缓冲(cap 200,EnterLobby 清),刷新重连成功后发 `__gateway_log_replay` 批量,浏览器 `logStore.prepend` 前插日志面板。自验:typecheck/build 绿,web 89/lua-native 12;refresh-test 实测重连 resync+请求重推正常。三服务运行供验收。


- 2026-06-08 **浏览器验收三 bug 修复(刷新被AI接管 / 遗计UI / 旁观视角固定)**(freekill-web 提交 a2c84a3 + 5d71677)。派 3 个 RC agent 对源码定位、主控复验后修:**#1 刷新即被 AI 接管+丢战报/请求 UI**(提交 a2c84a3,网关):根因——网关在浏览器 WS 关闭时立即 `asio.close()`,asio `onDisconnected` 立刻 `setRunned(true)`(serverplayer.cpp:188)转 Run 态→请求循环 800ms 内交 AI。修:WS 关闭不再立即 close,按 uuid **park** asio 25s 宽限(asio 仍 Online→不接管);同 uuid 返回登录走正常 fresh login→asio 原生 reconnect(auth.cpp:467 踢旧+全量 Reconnect 重发→空 VM 重建+待应答请求重推)。**实测 refresh-test.mjs:A 在 AskForGeneral 时刷新→A2 收 Reconnect+AskForGeneral,asio 日志确认 "reconnected"**。顺带修了浏览器 StrictMode 双连+重连风暴自我限频(doConnect 防重入、限频/认证失败不重连、登录成功不计入限频)。**#2 遗计 expand_pile 卡牌不可见**(提交 5d71677):VM 经 _new CardItem+ui_data.reason="expand" 发来 2 张牌,web 丢 ui_data、CardLayer 只渲染 cardStore 区内牌→无处显示。修:interactionStore 收 expandCards,CardLayer 注入手牌区+footnote 可点,vmStore 补取面。覆盖所有 expand_pile/私人牌堆请求。**#3 旁观视角固定**:原版点头像调 ClientInstance:changeSelf(纯客户端),web 无此 UI/不处理 ChangeSelf。修:clientVm 加 __fkChangeSelf 桥,vmStore switchViewpoint+消费 ChangeSelf 重排座位,gameStore observing 标志,Photo observing 时点击切换视角。自验:typecheck/build 绿,web 89/lua-native 12 全绿。三服务运行供再验收。


- 2026-06-08 **CustomDialog/MiniGame/QmlMark Web 还原方案分析 → `分析/CUSTOMDIALOG_MINIGAME_方案.md`**(纯分析,未改码)。回应"I-5 无法还原有无解法 + 规划要导入更多扩展包"。读源码(fk_ex/engine/client_util/各扩展包)厘清这其实是**三类机制**,可还原性差别大:**①QmlMark**(addQmlMark,极广用)——多数 `qml_path=""` 纯文本(`how_to_show` 算字符串),web 现**完全未消费**,加 `__fkGetQmlMark` 桥即可渲染,**零 qml 依赖、低成本可还原**;**②CustomDialog**——契约与已移植的 PoxiBox/ArrangeBox 同构,且多数扩展包**复用 `utility/qml/` 的有界共享框集合(~18 个/2219 行)**而非各自全新 qml,可逐个按 I-1~I-6 流程移植;**③MiniGame**——简单"多人选择"型可做通用组件+消费 UpdateMiniGame,复杂独立游戏(chess/poker 整包自带 RoomScene)才真正范围外。**结论:"无法还原"措辞应收窄**——真正无法的只有包专属复杂 qml/独立游戏模式;QmlMark+utility 共享框+简单 MiniGame 都可还原,是有界工作量。已把阶段 A(QmlMark 文本型 + utility 共享框)纳入 `M3-M6_detailed_plan.md` 的 M5「1b」节;I-5 的 unsupported 兜底重新定位为"未覆盖项安全网"(随 M5 推进缩小),非天花板。


- 2026-06-08 **M4 切片 I 审计修复:8 处还原错误 + cancel/锁定缺口全修**(freekill-web 提交 b795bc7,分支 m4-i-audit-fixes)。据 `分析/AUDIT_M4_I.md` 逐项对照 QML/Lua 修复:**①I-6 拖拽框初始预置牌序**(服务端 cards 是已按区分组二维 cardMap,QML 预置入各区→观星默认保序;popupStore 三 arrange 命令保留 initialSlots 不再 flatten,ArrangeBox 从 initialSlots 初始化、空 tray)**②I-6 is_free/pattern 约束**(!isFree 锁区0原始牌、pattern 不符牌锁定;新 `__fkCardFitPattern` 桥)**③I-1 Poxi shuffleInvisibleOutput**(新 `shuffleInvisiblePoxi` 多选去重随机,照搬 PoxiBox.qml:206-233,暗牌不泄露)**④I-3 双将名拆分翻译**(`trGeneral` 按 / split 逐段 tr)**⑤I-3 virt_name**(新 `__fkVirtualEquipNames` 桥按 owner+cid,vmStore moveBoard 解析,MoveBoardBox 叠加虚名)**⑥I-4 combo/cardname 忽略 choices 子集**(cycle 仅在 enabled 内循环)**⑦I-4 checkbox 不回报初始 []**(加 useEffect 播 [])**⑧I-4 cardname 读错默认字段**(读 default_choice);+ ArrangeBox cancelable 取消键、CheckInteraction 清空键。新 VM 桥均对真 freekill-core VM 验证。**自验**:typecheck/build 绿,web 88(arrange 预置/锁定/shuffleInvisiblePoxi 等新测试)、lua-native 12(新桥真 VM 断言);start-game 回归对真 asio PASS;asio/gateway/vite 三服务运行供浏览器验收。**至此 M4 切片 I 经审计达 1:1 还原**(除 I-5 CustomDialog/MiniGame 无法还原已合理兜底、若干呈现层简化可接受)。下一步:M4 切片 V(视觉动画)。


- 2026-06-08 **M4 切片 I 全面 1:1 还原审计 → `分析/AUDIT_M4_I.md`**(纯审计,未改码)。对 6 项交互补全派 5 个只读 audit agent 逐项对照 QML/Lua 源码,主控对高风险结论亲自读源码复验。**结论:核心交互逻辑大体忠实(I-2 CardsAndChoice 近乎完美 1:1),但发现 8 处还原错误 + 若干缺失,数项与切片 I「不放过非法选择/不空转」初衷抵触。** P0(还原错误):①**I-6 拖拽框丢失初始牌序**——服务端 `cards` 是已按区分组的二维 cardMap,QML 预置入各区(观星默认保序),web `flattenCards` 拍平成空 tray 强制手动重排(观星默认行为错误,已复验 room.lua:1713/popupStore.ts:234);②**I-6 忽略 is_free/pattern 约束**——命令携带 is_free(room.lua:1721/1820)/pattern,web 全不读,可产出服务端拒绝的非法排列(同 PoxiBox 当初要消除的降级);③**I-1 Poxi 未做 shuffleInvisibleOutput**——暗牌场景泄露所选背面牌+线协议偏差(PoxiBox.qml:118 OK 经 shuffle,web 直接 resolve(picked))。P1:④I-3 双将名未按 '/' 拆分翻译(显示拼音)⑤I-3 virt_name 丢失(虚拟装备显真名)⑥I-4 combo/cardname 忽略 choices 启用子集(可选禁用项)⑦I-4 checkbox 不回报初始 []⑧I-4 cardname 读错默认字段;+ I-6/I-4 cancelable 取消键缺失(防空转,与 I-5 一致性)。无法还原:I-5 CustomDialog/MiniGame(无 QML 运行时,unsupported 兜底合理)。详分级表与修复优先级见审计文档。下一步:按 P0→P1 回炉修复(建议进切片 V 前)。

- 2026-06-08 **M4 I-6 真拖拽框 + 切片 I 收尾**(freekill-web 提交 7611a04)。切片 I 最后一项:ArrangeBox(观星 Guanxing/排牌 ArrangeCards/交换 Exchange)原为"点选后再点区域"降级版——分组正确但区域内卡牌停在点击顺序,无法排序;而 ArrangeCardsBox.getResult(:414) 返回 [[cids 按序]],**区域内顺序有意义**(观星=置牌堆顶先后)。源码核实后改 Pointer Events 真拖拽:抽纯 reducer `arrangeDrop.ts`(arrangeDrop 原子移动+按 index 插入+超容量 bump 最旧卡回待分配区;arrangeValid 全放且各区 [limit,capacity]),ArrangeBox 重写为单一 {slots,tray} 态 + onPointerDown/Up 命中目标区与插入 index、拖动中半透明虚框,reply=slots 按序。**自验**:arrangeDrop.test.ts 7 测试(区域间移动、区域内插入保序=观星顺序、重排、超容量 bump、回待分配、validity);typecheck/build 绿,web 86/lua-native 12 全绿。**至此 M4 切片 I(交互补全)6 项全部完成**(I-1 AskForPoxi 真实化/I-2 CardsAndChoice/I-3 MoveCardInBoard/I-4 Interaction 子面板/I-5 CustomDialog-MiniGame fallback/I-6 真拖拽),对局内不再有"计时器空转/放过非法选择"的请求。下一步 M4 切片 V(视觉动画:vmStore 视觉命令总闸→animationStore + 声明式动画库 + 音频)。**遗留**:浏览器真机验收(M3 重连/旁观 + M4 各交互弹窗/拖拽眼见)与 M3 合并待用户。

- 2026-06-08 **M4 I-5 CustomDialog/MiniGame 最小 fallback**(freekill-web 提交 c831885)。审计要求:二者在 ACTIVATE_COMMANDS(启动计时器)但无 handler→空转无应答。源码核实(RoomLogic.js:1478/1488):popupBox.source=AppPath+path/qml_path 加载任意扩展 QML——Web 无 QML 运行时且属扩展包专用,无法支持。策略(audit:明确不支持+阻断空转):popupStore 加 unsupported kind,渲染最小提示弹窗+「跳过」键→resolve('__cancel') 既回服务端应答又 deactivate 计时器。**自验**:web +1 测试(两命令→unsupported,resolve 回 __cancel 关闭);typecheck/build 绿,web 79/lua-native 12 全绿。下一步 I-6 真拖拽框(切片 I 最后一项)。

- 2026-06-08 **M4 I-4 动态 Interaction 子面板**(freekill-web 提交 bc9690b)。审计 P4-020/P5-025(interactionStore 注释提到 Interaction 但 applyChange 从不保存,SkillInteraction 子面板缺失)。源码核实(Room.qml:781-836 + SkillInteraction/*.qml):Interaction 是 UpdateRequestUI change 的 `_new`/`_delete` 项(type="Interaction",data={spec,skill_name}),走 ui_emu 循环回报 `updateRequestUI("Interaction","1","update",value)`(非弹窗);四子类型 combo/cardname(选 all_choices)、spin([from,to] 整数)、checkbox(选 min..max)。interactionStore 加 interaction 字段(InteractionSpec 类型),`_new` 捕获 Interaction 存 spec+skill_name、`_delete` 清除,其余行为不变;Dashboard active+interaction 时渲染 InteractionPanel 分派到 Combo/Spin/Check 子组件(各自 hooks 不条件化、key=type),initial default 经 useEffect 上报(对齐 QML item.clicked()),值变走既有 interact 出口;custom(扩展 QML)渲染空。**自验**:interactionStore +1 测试(_new 存 spec/skill、_delete 清除、无关 change 不影响);typecheck/build 绿,web 78/lua-native 12 全绿。下一步 I-5 CustomDialog/MiniGame 最小 fallback。

- 2026-06-08 **M4 I-3 AskForMoveCardInBoard 补全**(freekill-web 提交 c974571)。审计 P5-024/P6-019(web room.lua 保留该请求但 popupStore 无处理器→计时器空转)。源码核实(room.lua:2973-2994 + MoveCardInBoardBox.qml + RoomLogic.js:1103):data={cards,cardsPosition[0|1],generalNames[两侧],playerIds},点牌预览移到对侧,reply={cardId,pos} 其中 **pos=牌的原始位置**(服务端据此决定 from/to)。无过滤规则、无需 VM 桥。popupStore 设 kind:'moveBoard' 留 mbCards/mbPositions/mbSideNames;RequestPopup 加 MoveBoardBox(两侧行、单选预览移到对侧、OK 选中启用、reply {cardId,pos:原始位置});vmStore 补 mbCards 取面。**自验**:web +1 测试(notify→state→reply 用原始 pos);typecheck/build 绿,web 77/lua-native 12 全绿。**修一项验一项**中两次因 Edit 非唯一匹配误删邻近代码(EmptyRequest 分支、CardsAndChoice 测试尾),均被测试当场抓出(EOF/false≠true)并立即补回——印证 build/test 闭环有效。下一步 I-4 动态 Interaction 子面板。

- 2026-06-08 **M4 I-2 AskForCardsAndChoice 补全 + dev server 起供 M3 浏览器验收**(freekill-web 提交 5658526)。①**dev server**:asio(WSL 172.29.119.214:9527)+ 网关(:9528)+ vite(:5173)三服务起好,供用户双标签页眼见验收 M3(重连/旁观)。②**I-2**(审计 P5-023,vmStore 列了 AskForCardsAndChoice 但 popupStore 无 case→计时器空转):照搬 ChooseCardsAndChoiceBox.qml + RoomLogic.js:1139——clientVm 加 `__fkChoiceFilter` 桥(`Fk.skill_skels[filter_skel].extra.choiceFilter(cards,choice,extra)`,镜像 QML:124 Lua.evaluate,无 skel 返 true);popupStore 设 kind:'cardsAndChoice' 留 cards/disabled/ok+cancel options/filter_skel/extra/min/max;RequestPopup 加 CardsAndChoiceBox(选牌 disabled 不可选/min..max,OK 选项 i 启用=count 在范围 &&(i===0 或 choiceFilter 通过),cancel 恒可用回空 cards,reply={cards,choice});vmStore 补 ccCards 取面。**自验**:web popupStore +2 测试(AskForPoxi 保留 poxi 规则 payload+visible_data;CardsAndChoice notify→state→reply),lua-native +__fkChoiceFilter 真 VM 测试(无/缺失 skel 返 true)。typecheck/build 绿,web 76/lua-native 12 全绿。下一步 I-3 AskForMoveCardInBoard。

- 2026-06-08 **审计结论处置:文档口径同步 + M4 切片 I 启动(I-1 AskForPoxi 真实化)**。据外部审计五条结论:①(低)主计划文档滞后——修 `freekill_web_implementation_plan.md`:`M3-M4_detailed_plan.md`→`M3-M6_detailed_plan.md`、M3 标题由「下一步重心」改「✅ 代码+脚本完成,浏览器视觉验收待补」、M4 标「下一步重心」,R1/R2 切片标 ✅ 并写明脚本级通过/浏览器级待补,与 PROGRESS 一致。②(中)M3 口径明确为 implementation/script PASS + browser acceptance pending(已在计划与 PROGRESS 双处写清)。③(中)R-CRED 明文凭据债已在风险登记册登记(M6 偿还)——确认无需新增。④(中高,真修)**M4 I-1 AskForPoxi 真实化**(分支 m4-i-interaction-completion 提交 b8b0ee2):popupStore 原把 AskForPoxi 降级 min0..maxAll 可放过非法选择;改为照搬 PoxiBox.qml+RoomLogic.js:1077,选择合法性走 VM `Fk.poxi_methods[type]` 的 card_filter/feasible/prompt——clientVm 加 `__fkPoxi` 桥(调 client_util.lua PoxiPrompt/PoxiFilter/PoxiFeasible),popupStore 设 kind:'poxi' 留原始 type/data/extra_data,RequestPopup 加 PoxiBox 组件(selectable=已选||poxiFilter,OK=poxiFeasible,reply=选中 cid 数组)。**自验**:lua-native 加真 VM 测试,对真实 AskForCardsChosen poxi 方法断言 feasible 拒 0 选(<min)/3 选(>max)、收 1~2 选;typecheck/build 绿,web 74/lua-native 12 全绿。⑤(低)M5 的 MD5 manifest(computeFlistMd5 仍 throw、网关硬编码)确认不阻塞 M4、留 M5 混连前置。下一步 I-2 AskForCardsAndChoice。

- 2026-06-08 **M3 切片 R2 完成:断线重连/旁观(核心)**(freekill-web 提交 942c94e,分支 m3-r2-reconnect-observe)。按 10 步工作流,**Step 0 自主起 WSL asio+网关**,**Step 3/4 先用 reconnect-probe.mjs 对真 asio 记录重连重发序列再实现**(纪律「首包先记录再抽象」)。**源码+实测核实重连协议**:在局玩家用同 user/password 重新登录 → asio `auth.cpp:465-479` 检测 `insideGame()` 的同名玩家(同 uid)→ `player->reconnect()`(serverplayer.cpp:232)→ Lua `ServerPlayerBase:reconnect`(serverplayer_base.lua:63)发 `Reconnect`(room serialize)+`RoomOwner`+state → 客户端 `loadRoomSummary` 重建全状态。**故重连=用持久凭据重新走登录,无需特殊重连协议**;R1 已能路由 `Reconnect` 包引导 VM。**探针实测**:2 人局 A 在 AskForGeneral 时掉线→重连,A2 收 `NetStateChanged/Setup/Reconnect/RoomOwner/PropertyUpdate`(PASS)。**关键实测教训**:必须 2 真人(非 1 人+bot)——独人掉线 asio 直接 `GameOver`(probe room 6 实测 GameOver in 3s 正好在掉线时刻),无可重连之局。**实现**(纯 web,gateway 无改):①凭据策略**用户拍板 localStorage 持久化+完全无感**——connect 存 `{url,user,password,uuid}`,App 挂载 `tryAutoLogin` 自动重登(survives 硬刷新/浏览器重开);安全债 **R-CRED**(明文密码,XSS 面)记入实现计划 §11,M6 换 session token,代码隔离 loadCreds/saveCreds/clearCreds。②自动重连——`onStatus 'closed' && !intentionalClose` 触发指数退避(1s→8s,≤10 次),'online' 清零;重连前 `useVmStore.reset()`+新增 `resetRoutingState()`(清 inRoom/loginSetup/feedChain/currentRequestId)使 asio 重发在干净 VM 重建。③App `reconnecting` 时保持房间视图+「正在重连」徽章,不弹回登录。④旁观只读 & 未应答请求**架构保证**(asio request loop 按 `_observers` 跳过非参与者;断线期 AI 接管应答,重连拿当前状态;serialize 不含 pending request),无需客户端特殊处理。**自验**:typecheck/build 绿,web 74 测试;三脚本对真 asio 全 PASS(start-game 回归/observe R1/reconnect R2)。**遗留真机点**:浏览器双标签页无感重连/刷新恢复待用户确认。**至此 M3 全切片完成**,下一步 M4 切片 I(交互补全)。

- 2026-06-08 **M3 R1 真机自验通过 + bring-up 纳入工作流**(freekill-web 提交 833715a)。**AI 自主**在 WSL Ubuntu 起 asio(`wsl-run-asio.sh` 后台常驻,IP 172.29.119.214:9527,v0.1.14 已构建)+ Windows 网关,对真 asio 跑 `observe-reconnect.mjs`。**修正脚本两处后 PASS**:① CreateRoom settings 补 `_game` 块(generalNum:3 等)——初版漏带致服务端 Lua `chooseGenerals` 撞 `generalNum nil` → GameOver in 0s(PROGRESS 2026-06-06 已记此坑,验证了那条记录);② 用独立大厅客户端发现 roomId(in-room 客户端 RefreshRoomList 返回空)。**结果**:观察者收到 asio 的 `Observe`+`AddObserver` 服务器包——正是旧 routeEnvelope 在大厅 default 丢弃、R1 现路由到 VM boot 的命令,**经验实证 R-ROUTE 真 bug 与修复前提成立(asio 对旁观发 Observe 非 EnterRoom,且仅对 running 房 room.cpp:330)**。start-game.mjs 回归同步复跑 PASS,asio/gateway 路径健康。**工作流优化**:把"WSL 起 asio+网关"定为工作流 **Step 0(联调切片 AI 自主完成,勿甩用户)**,在 `M3-M6_detailed_plan.md` 新增「本地联调环境 bring-up」节(取 IP/起 asio/起网关/自验脚本/4 个实证坑/真机点),memory [[asio-wsl-runtime]] 已含细节。**R1 至此静态+真机双绿**,下一步切片 R2(断线重连/旁观)。

- 2026-06-08 **M3 切片 R1 完成:修复大厅路由丢包 + 带密旁观 + 退房确认**(freekill-web 提交 79c515d,分支 m3-r1-routing-reconnect-prep)。严格按新定 10 步工作流:读状态→规划→**源码查询纠正初稿**→实施(切片分支)→源码对照→静态自验→git→sync。**源码查询阶段纠正了一处规划偏差**:asio 服务器对旁观/重连发的是 `Observe`/`Reconnect` 命令(`room.cpp:346`、`serverplayer.cpp:246`),Lua 客户端经 `loadRoomSummary` 才**再** emit `notifyUI("EnterRoom")`(`clientbase.lua:470`)+`startGame()`——这是 VM 输出走 vmStore→gameStore 的另一条 sink,非 routeEnvelope 见到的服务器包,故泛化 boot 触发不会二次引导;且 `reconnect` 先 emit 的 `notifyUI("EnterLobby")`(`:496`)不经 routeEnvelope、gameStore 无该 case,不会误拆房。**四项修复**:①路由引导泛化——抽出 `stores/roomRouting.ts` 的 `isRoomBootstrap`(EnterRoom/Observe/Reconnect),routeEnvelope 用它替代独认 EnterRoom;②大厅应答 Heartbeat——`useConnectionStore.getState().client?.notify('Heartbeat','')` 重置 asio ttl(`server.cpp:64-90` 每 30s 减 ttl/max_ttl=6,`serverplayer.cpp:170` 收 Heartbeat 重置,大厅 VM 未引导故须前端补);③带密旁观——`RoomList` 抽 `promptPassword`,observe 与 enter 一致(asio `joinRoom` enter/observe 同密码门 `lobby.cpp:240`,空密码撞 `room password error`);④对局中退房二次确认——`LobbyPage` 房间条 `tryQuitRoom`:started 时 `window.confirm`,等待房间/旁观直退(照搬 `RoomPage.qml:296-311` tryQuitRoom)。**自验**:`pnpm -r typecheck/build` 绿;web 74 测试(新增 `roomRouting.test.ts` 3 个回归:三命令引导、非引导命令、命令集);新建 `apps/gateway/scripts/observe-reconnect.mjs`(双 WS 旁观自验脚本,需 WSL asio 运行,留作 R1 真机验收点)。纯静态验证已尽,真机双 WS 验证(B 旁观见 A、带密旁观、大厅挂机不被踢)待用户在 WSL asio 环境跑脚本/双标签页确认。下一步切片 R2(断线重连/旁观)。

- 2026-06-08 **独立审计合理性复核 + 据此重写推进计划**。①**复核**:对 `audit/` 下 12 份逐元素对照报告(phase1-9,~296 条结论)派 12 个只读 agent 二次核查——各自重开报告引用的 FreeKill-sourcecode 源码与 freekill-web 实现(拷贝 Lua 用 `diff`/`git diff --no-index` 实差、缺失类用全仓 grep 反证、资产类磁盘 ls 实测),独立判定每条 status 是否成立。结果 **286 AGREE / 7 PARTIAL / 3 UNVERIFIABLE / 0 DISAGREE**,产出 `分析/AUDIT_REPORTS_META_REVIEW.md`(总评+可微调条目+跨报告高价值缺口+逐 Phase 明细表)。结论:这批报告高度可靠、判级校准准、能区分"代码已拷贝≠运行时已还原"。可微调处仅判级措辞偏严(P4-013/P5-011/P7-026)、个别行号/路径误引、P8-029 数字(28≠30),不影响缺口结论。②**重写计划**(本次纯文档,未改生产码):据审计+用户决策重排里程碑——M2 标 ✅核心达成;新增 **M3 路由修复+断线重连/旁观(核心,下一步重心)**、**M4 交互补全+视觉动画**、**M5 扩展UI/i18n/MD5/混连**、**M6 生产化**;**回放/录像/战绩、大厅设置/建房丰富度标记延后/可选**(用户拍板)。改 `freekill_web_implementation_plan.md` §9 里程碑/§10 验收/§11 风险(新增 **R-ROUTE 高·M3 首修**、R-ANIM 降级已实现);新建 `分析/M3-M6_detailed_plan.md`(逐切片:先读源码→做→自验→验收,挂审计条目编号);**删除过期 `M2_detailed_plan.md`**(M2 不再是当前阶段)。③**头号真 bug 群(R-ROUTE)**:审计实证 `routeEnvelope` 只认 `EnterRoom` 引导 VM,大厅阶段 `Observe`/`Reconnect` 首包与 `Heartbeat` 落 `switch default` 被丢→旁观/重连 broken、大厅久挂可能被踢;与断线重连同根因,故 M3 合并 R1(修路由/心跳/带密旁观/退房确认)+R2(重连/旁观)。

- 2026-06-07 **【严重】jsonSanitize 指数爆炸导致 2GB OOM 崩溃**(freekill-web)。现象:满人局触发"Cannot enlarge memory, requested 2157312240 bytes"(clientVm.ts:275 feedPacket),VM 当场死亡,连带五谷丰登选牌 popup 不弹(同根因——VM 崩溃后 feed chain 断裂,FillAG/AskForAG 等后续 notify 全部不再处理)。根因:上一处 notifyUI 循环引用修复里的 `jsonSanitize` 在递归出栈时 `seen[v] = nil`,只能检测**当前 DFS 路径**上的环,而真实满人局对象图是稠密 DAG(room→每个 player→room→每个 player…),每个共享节点沿每条路径重复展开 → 指数级 → 2GB。2 人无懈测试图太小没暴露。修复:`seen` 改为**全程永久**(不出栈清除),把遍历界定为 O(不同对象数) 并对共享子树去重;标量不入 seen(原样返回),故客户端真正要读的标量数组(card-id 列表等)绝不截断,只有客户端忽略的循环 player-object 数组被剪。**自验**:新增 `sanitize-dense-graph.test.ts`——构造 6 player×room 全交叉稠密图 + 五谷式 card_data 标量列表,断言 318ms 完成(不再 OOM)且 `[23,126,90,7,12]` id 列表完整保留;lua-native 11 测试全绿。**已 sync-assets** 把修复同步到 public/fk/fkprelude.lua(浏览器实际加载的副本,grep 确认旧 `seen[v]=nil` 已消失)。reload 浏览器生效。

- 2026-06-07 **回房间按钮失效(已修) + 多人选将不一致(加固+埋点待验)**(freekill-web)。①**游戏结束"返回房间"后所有人看不到人数/加机器人/开始**:旧"返回房间"只调 `resetGame()`(纯本地清空 players+capacity=0),而 asio 在 GameOver 后不会重新广播房间名单,WaitingRoom 因此空白。QML 走 `RoomPage.resetRoomPage → Lua ResetClientLua`(client_util.lua:547:用保留的 cpp players+enter_room_data 重建 ClientInstance,保留 owner/ready,重读 capacity)。修复:clientVm 加 `__fkResetClient`(调 ResetClientLua,返回 capacity);gameStore 加 `backToRoom(capacity)`(清 started/winner+设 capacity,**不清 players**);GameOverModal "返回房间" 改为 `vm.resetClientLua()` → `backToRoom(cap)` → `syncPlayers(readPlayers,false)` 重灌名单。**自验**:lua-native 跑通 ResetClientLua(capacity/players 保留)。②**多人真人选将拿到默认/随机武将**:逐源码确认非主公选将走批量 `Request:new(nonlord,"AskForGeneral")`+`setDefaultReply`(gamelogic.lua:127-139),回复未匹配到 pending request(requestId 不符)→ asio 取默认随机将(request.lua:357-360)。旧实现 `client.reply(0,data)`+gateway 用 `lastRequestId` 猜;改为**客户端按所答请求的真实 requestId 回显**:routeEnvelope 捕获 request 包的 `currentRequestId`,reply senders 用它;ws-bridge 改为 `env.requestId || lastRequestId`(非 0 即尊重客户端)。**注**:静态分析未 100% 锁定多人 requestId 必然错配(单连接内 asio 不会插入新 request),故此为加固;已加 `[REPLY-DBG]` 日志(request/popup/VM reply 的 id+data),待用户复现后据日志最终确认。typecheck/web 71 测试全绿,gateway 已 rebuild+重启(命令:`cd apps/gateway && ASIO_HOST=172.29.119.214 node dist/index.js`,task belhyccf3)。**遗留**:确认 #1 真因后移除 4 处 REPLY-DBG 日志。

- 2026-06-07 **坐骑图标缺失 + 显示马名**(freekill-web)。两问题:①**有些坐骑不显示马图标**:`horse.png` 只存在于 standard_cards 包,但 EquipArea 旧 `equipIcon(name,ext)` 只按卡牌自身 extension 拼一条路径,非 standard_cards 来源的坐骑 → 404 → `hideImg` 直接隐藏(SkinBank.getEquipIcon 本应回退:卡牌 ext → 扫所有包 → 内置 unknown)。修复:skin.ts 加 `equipIconCandidates`(镜像 getEquipIcon 顺序:ext → ART_PKGS 扫描 → `/fk/image/card/equipIcon/unknown.png`),EquipArea 用 `walkIcon` 在 `<img> onError` 时逐个回退,耗尽才隐藏。②**+1/-1 后显示马名**(用户要求):旧逻辑坐骑 label 硬编码仅 `+1`/`-1`(照搬 EquipItem.qml:151/155);改为 `${+1/-1} ${tr(virt_name||name)}`(如 "+1 的卢"),马名翻译走 vmStore:339 卡面 name/virt_name 注册路径。typecheck/build 干净,web 71 测试全绿(skin.test 加 equipIconCandidates 回退用例)。纯 web 代码,HMR 即时生效,无需 sync-assets。

- 2026-06-07 **"b" 提示真因——notifyUI 序列化遇循环引用回退成 `tostring`**(freekill-web)。靠浏览器 `[PROMPT-DBG]` 日志抓到铁证:`UpdateRequestUI localized="是否为目标为 华佗 的【闪电】使用【无懈可击】？"`(正确),但 `AskForUseCard data="table: 0x7895e0" → cardname=t prompt="b"`。根因:`CppClient:notifyUI`(fkprelude.lua:95)做 `json.encode(data)`,失败时回退 `tostring(data)`=`"table: 0x..."`,客户端按字符索引→`data[0]`="t"、`data[2]`=**"b"**("ta**b**le" 的 b)。为何 encode 失败:无懈可击询问的 `extra_data.players` 是**活的 Player 对象列表**(player↔room 循环引用,cardskill.lua:372),lib/json.lua:64 抛 "circular reference"。Qt/QML 客户端不踩坑因其 variant 编组也带不动活对象,客户端只读 plain id/string/number。修复:fkprelude 加 `jsonSanitize`+`safeEncode`——encode 失败时在**消除循环/丢弃 function/userdata** 的副本上重试,且**镜像 json.lua 的数组/对象判定**(有 `[1]`→密集数组 1..n 遇 nil 即止;否则只留 string 键的对象),避免触发 json.lua 的 "mixed key types"/"sparse array";最后兜底返回 `"{}"` 而非 table 串。同步移除 vmStore 临时调试日志。**自验**:lua-native 加回归测试——构造带循环 player 对象的无懈可击 AskForUseCard payload,断言 notifyUI emit 的是可解码**数组**(`[0]`=nullification、`[2]`=完整 prompt、extra_data 标量字段保留、循环被剪),lua-native 10 测试全绿。web 70 测试、全栈 typecheck/build 全绿。需浏览器 reload 重载 prelude。**注**:之前误判此问题为 prompt 本地化/缺省逻辑,实为底层序列化 bug,影响所有含活对象的 notify。

- 2026-06-07 **铁索连环缺重铸选项 + 出牌结束键遮挡 photo(对真实 VM 验证)**(freekill-web)。逐行读源码定位两问题:①**SpecialSkills(重铸/正常使用单选)从未渲染**:铁索连环 `special_skills={"recast"}`(maneuvering/pkg/init.lua:38),出牌阶段选中时 ui_emu(play_card.lua:194-203 ReqPlayCard:selectCard)前插 `_normal_use` 并推 `SpecialSkills[{id:"1",skills:["_normal_use","recast"]}]`;QML 渲染为 RadioButton 组(Room.qml:453-465,`Lua.tr(modelData)` 文本,点击 `updateRequestUI("SpecialSkills","1","click",modelData)`)。我方 interactionStore 存了 specialSkills 但**无组件渲染**。修复:Dashboard 加单选组(显示条件照搬 Room.qml:437-449:count>1 或单项非 `_normal_use`),首项默认选中(`checked:index===0`),点击经 interact 路由(`JSON.stringify("recast")`→Lua `json.decode`→`selectSpecialUse(data)`,链路实测已通);vmStore UpdateRequestUI 分支注册 specialSkills 翻译键(_normal_use→正常使用/recast→重铸)。②**出牌阶段结束键遮挡 photo**:上轮把结束键放 `right:30 bottom:40`,但实测自己 photo 在压缩 1200×540 舞台占右下角 x[1033.6..1164.8] y[348..522.8],正被覆盖。改放 OK/取消 同高、photo 左缘左侧 `right:175 bottom:122`(x[935..1025],与 photo/居中OK取消/低条技能列均不冲突)。**自验**:lua-native 加真实 VM 测试——boot 客户端→出牌请求→`UpdateRequestUI("CardItem",<iron_chain>,"click",{selected})`,断言 emit 的 SpecialSkills 含 `recast`+`_normal_use`(lua-native 9 测试全绿,+1)。web 70 测试、全栈 typecheck/build 全绿。**三服务仍在运行供浏览器实测**(asio:9527/gateway:9528/web:5174)。

- 2026-06-07 **技能 prompt 仍缺失——真因定位+修复(对真实 captured packet 验证)**(freekill-web)。上一轮加了 PlayCard/AskForUseCard/AskForResponseCard/AskForUseActiveSkill 的缺省 prompt,但漏了 **AskForSkillInvoke**(触发技 洛神/倾国 等的询问)。逐行读源码定位:`ReqInvoke`(invoke.lua)**自身不设任何 prompt**,完全依赖 `AskForSkillInvoke` notify;client.lua:807 `askForSkillInvoke(data)` 注释称 data=`[name, prompt]`,但**对 captured-packets.json 实测**该请求 data 多为**单元素 `["luoyi"]`(仅技能名,无 prompt)**(CBOR 字节串解码确认,见 [[cbor-x-asio-gotchas]])。原 web handler `setPrompt(localizePrompt(vm, d[1]||''))` → `localizePrompt('')` → 空串 → 空 bar。QML 对应回调(RoomLogic.js:829-830)正是 `prompt ? processPrompt(prompt) : Lua.tr("#AskForSkillInvoke").arg(Lua.tr(skill))`("你想发动〖%1〗吗？")。修复:AskForSkillInvoke 分支改为取 `skill=d[0]`,空 prompt 时走 `defaultPrompt(vm, '#AskForSkillInvoke', skill)`(Qt `.arg()` = `%1` 替换,VM 翻译 #AskForSkillInvoke + 技能名)。typecheck/build 干净,web 70 测试全绿(processPrompt 套件加 #AskForSkillInvoke `%1` 替换用例 + luoyi→洛神)。**本地三服务(asio WSL:9527 / gateway:9528 / web vite:5174)已起,HMR 已载入修复供浏览器实测。**

- 2026-06-07 **出牌栏三问题修复(操作按钮遮挡手牌 / 缺省 prompt 缺失 / 手牌溢出核查)**(freekill-web)。逐行读 QML 源码定位:①**结束键遮挡手牌**:原版 OK/取消 是 `controls` 内居中 Row(`okCancel`,锚 `progress.horizontalCenter`,整个 `controls` 锚 `dashboard.top bottomMargin:-60` 即**位于手牌区上方**),而**结束键是独立按钮**(`endPhaseButton`,Room.qml:510-519,`anchors.right rightMargin:30` 贴右下角);我方把三键都居中放 `bottom:50`,出牌阶段只剩"结束"时正好压在中间手牌上。修复:Dashboard 拆分——OK/取消居中上移到 `bottom:122`(手牌带顶 `STAGE_H-96=bottom:96` 之上),结束键移到右下角 `right:30 bottom:40`;倒计时条 `bottom:150→166`、prompt `bottom:104→190` 整体上移避让,垂直顺序对齐 QML(下→上:okCancel→倒计时→prompt)。②**技能/响应请求缺 prompt**:原版请求回调(RoomLogic.js:1172/1204/1225/1259)在**服务器 prompt 为空**时显示缺省 `Lua.tr("#AskForUseCard").arg(牌名)`("请使用【杀】")/`#AskForResponseCard`("请打出【%1】")/`#AskForUseActiveSkill`("请发动〖%1〗")/`#PlayCard`("出牌阶段，请使用一张牌");ui_emu 对这些无显式 prompt 的请求 `UpdateRequestUI._prompt` 发**空串**(response_card.lua:36 `original_prompt = prompt or ""`),QML 靠 `if (uiUpdate["_prompt"])` **真值守卫**(RoomLogic.js:1567)丢弃空串、保住缺省。我方两处都缺:vmStore 加 `PlayCard/AskForUseCard/AskForResponseCard/AskForUseActiveSkill` 显式分支(空 prompt→`defaultPrompt()` 走 VM 翻译 `#AskFor*` + `%1`←牌名/技能名,非空→localizePrompt);interactionStore `applyChange` 的 `_prompt` 合并加真值守卫(空串不覆盖,让请求命令设的缺省留存)。事件序:client.lua 先 `setupRequestHandler`(→空 UpdateRequestUI 被守卫丢)再 `notifyUI(command)`(→设缺省),故缺省胜出,与 QML 一致。③**手牌溢出**:核查确认**无 bug**——CardLayer.tsx:56-57 `step=min(CARD_W+6, span/(n-1))` 已是 `ItemArea.updatePosition`(ItemArea.qml:36-47 溢出时 `spacing=xLimit/(n-1)`)的忠实移植,牌数多时自动压缩间距落在 `box.w` 内,不会溢出舞台。typecheck 干净、build 干净、web 69 测试全绿(新增 interactionStore 空 prompt 守卫用例)。

- 2026-06-07 **prompt 富文本渲染修复(手动验收发现 `<br />` 字面量)**(freekill-web)。截图显示借刀杀人等锦囊的请求提示把 `<br />` 当字面量显示。读全源码定位真因:原版 prompt 栏 `Room.qml:378` 用 `textFormat: TextEdit.RichText`(GraphicsBox 标题同),`<br/>`/`<b>`/`<font>` 等会渲染为富文本;我方两处渲染点(Dashboard 底栏 prompt、RequestPopup 弹窗 Modal)用 React `{prompt}` 纯文本输出会转义 HTML,故显示字面量。修复:①新建 `PromptText.tsx`——`dangerouslySetInnerHTML` + 复用 GameLogPanel 同款 sanitize(剥 script/on*=/javascript:),对齐 RichText 语义,prompt 由本地 VM 产出属可信计算、sanitize 仅纵深防御;②Dashboard.tsx、RequestPopup.tsx 两处 prompt 渲染改走 `PromptText`;③Dashboard prompt 样式 `whiteSpace:nowrap→normal`,长提示在 maxWidth 内换行(原版无宽度上限自增长,固定舞台下换行为合理等价,`<br/>` 仍强制其预期断行)。**另对齐 RoomLogic.js 每个 box 都 `processPrompt`**:此前 ui_emu 路径已 localizePrompt,但 popup 类请求(AskForChoice/Choices/CardChosen/CardsChosen,QML 在 RoomLogic.js:943/977/1012/1051 均 processPrompt)的 prompt 入 popupStore 时未处理——加 `popupStore.setActivePrompt` + vmStore 在 `handle()` 后对 active.prompt 跑 localizePrompt(翻译键 + `%src/%dest/%arg` 插值 + 富文本),对已是中文字面量的 handler('请选择武将' 等)幂等。typecheck 干净、web 68 测试全绿(processPrompt 5 + popupStore 14 等未回归)。

- 2026-06-06 **6.5 修复批次 3(Photo/卡牌/弹窗 P1 主体,5 桶)完成**(freekill-web)。严格按工作流(QML 源码绝对优先、修一项验一项、关键 VM 桥对真实 VM 验证、各桶独立提交):①**D 桶卡牌**(b9826b1):D1 虚拟牌名 snow 框(CardItem.qml virt_rect)、D2 卡牌 @-mark 渐变红 pill(cardMarkDelegate,readCards 加 GetCardData.mark)、D4 禁用改半透明黑罩(BasicCard disable rect 非 brightness)、D5 选中改 chosen.png(BasicCard chosen 非黄框);lua-native 验 mark 为数组。②**B 桶 Photo 子区**(613dbeb):B5 MarkArea @-标记文字(读 displayMarks)、B20 装备槽封印灰罩(EquipItem #CCC,槽→Player.<X>Slot 映射)、B21 判定槽封印图(DelayedTrickArea sealed);skin 加 chosenPic/delayedTrickSealedPic。③**F 桶结算**(a7fc62c):F1 GameOver 逐玩家结果表(将/名/胜负/身份/回合/回复/伤害/受伤/击杀,读 GameSummary banner 拼 general/role)、F2 折叠 toggle、F3 返回房间按钮(resetGame);clientVm 加 __fkGameSummary 桥。④**C 桶技能**(6b051c4):readSkills 改返回分类 SkillInfo{orig,name,freq,frequency}(GetMySkills+GetSkillData),Dashboard 按 SkillArea.qml 分 active(可点)/notactive(锁定灰),limit/wake/quest 加限/觉/任标签;lua-native 验 jianxiong.freq=notactive。⑤**E 桶弹窗**(d193241):E4 TakeAG 改为保留已取牌+灰化+取者名(AG.qml takeAG 非移除),agCards→[{cid,takenBy}];E13 cards 弹窗加 cancelable 取消键。全程 web 61/lua-native 8/build 绿。**批次内延后(需新数据通道)**:D8 脚注/D9 桌牌淡出、B1 限定技/B4 距离/B15 HandcardViewer、C9 次数徽章/C18-22 交互子面板、E7/E9/E10/E11/E12 弹窗、F8 房内聊天——记入待办批次 5。下一步批次 4(动画=切片7)。

- 2026-06-06 **6.5 修复批次 2(三大点名系统主体,3 项)完成**(freekill-web)。严格按工作流(QML 源码优先、修一项验一项、关键 VM 桥对真实 VM 验证、各独立提交):①**TMR3/B12 per-Photo 思考进度条 + MoveFocus**(7f2231b):vmStore 此前丢弃 MoveFocus,现消费 `[ids,command,timeout]`(实测 roombase.lua:225 服务端正是此形),focusStore 按 cancelAllFocus 语义每次替换 focus 集,PhotoFocusBar 渲染底部 4px 倒计时条+「<command> thinking...」提示(照搬 Photo.qml:359-398),到点仅隐藏不回复;command+" thinking..." 走 VM i18n。②**GEN12/13/22 选将接 VM 规则**(bc489ff):GeneralBox 弃用固定数量判定,改调 VM chooseGeneralPrompt/Filter/Feasible(照搬 ChooseGeneralBox.qml:230-237,303→client_util.lua),非选候选灰化禁用;popupStore 带 ruleType/extraData/hegemony/convertDisabled;clientVm 加 __fkChooseGeneral 桥(单次 JSON 往返);lua-native 实测 feasible(0)=false/feasible(1)=true/filter=true。③**DET1/2/3 右键武将详情**(021b352,用户点名第三系统):右键 Photo→detailStore→GeneralDetailModal(照搬 PlayerDetail.qml 核心:武将立绘 GeneralCard + 可见技能名/描述),技能取自 VM GetPlayerSkills(client_util.lua:399,Self 见全部可见技、他人隐藏装备/&技);clientVm 加 __fkPlayerSkills 桥;lua-native 实测桥返回良构数组。**至此三大用户点名系统(计时/选将/右键武将详情)主体全部可用。** 全程 web 60/lua-native 8/build 绿。下一步批次 3(Photo/卡牌/弹窗 P1)。

- 2026-06-06 **6.5 修复批次 1(全部 P0,5 项)完成**(freekill-web)。严格按工作流(以 QML 源码为准、修一项验一项、各独立提交):①**E6/E8 选项/复选竖列布局**(e4ba99d):照搬 ChoiceBox/CheckBox.qml `GridLayout flow:TopToBottom rows:8`(CSS grid-auto-flow column),渲染全部 all_choices 并禁用非 choices 子集——顺带修掉旧代码 map options 却按位 index values 的潜伏错位 bug。②**G6 盲选混淆**(4ebecef):照搬 PlayerCardBox.qml shuffleInvisibleOutput,单选背面牌时从同区背面集随机返回一张 cid(防泄露所选),纯函数+注入 rng+测试。③**G3 身份可见性接 VM**(d4af5f9):弃用硬编码 role_shown 启发式,VM 桥 readPlayers 新增 roleVisible=Self:roleVisible(p)(player.lua:1699),Photo.shownRole 照搬 RoleComboBox.qml 表达式;lua-native 实测 Self:roleVisible(Self)=true。④**GEN1/GEN2 选将立绘+势力框**(6652a23,用户点名):新建 GeneralCard 照搬 GeneralCardItem.qml(立绘+border 框+势力图标+竖排名),替代纯文字按钮;vmStore 为 AskForGeneral 候选拉 readGenerals(extension+kingdom)。⑤**TMR 计时系统**(5b4cc35,用户点名,含 TMR1/2/5/9/10):照搬 Room.qml progress ProgressBar——请求 envelope 的 timeout+timestamp 启动倒计时,CountdownBar 渲染 60% 橙→红渐变条,到点调 VM FinishRequestUI(**仅 UI 清理不回复**,服务端拥有真超时;已核实 _finish 不发 reply);timerStore 持 deadline,vmStore 起止;clientVm 加 __fkFinishRequestUI 桥。全程 web 57/lua-native 8/build 绿。

- 2026-06-06 **切片 6.5 全面 UI/系统审计完成 → `分析/AUDIT_6.5.md`**(freekill-web 提交 bb4ba39 收尾切片6)。先验证并提交切片6遗留改动(cardPicCandidates 候选链修 jink-no-art 闩锁、JudgeArea 按名分组+计数徽章、ArrangeBox 接真卡面;web 48/build 绿)。然后严格按工作流执行 6.5:**11 个只读 audit agent 并行通读 QML 源码**(A 牌桌布局/B Photo 子区/C 手牌技能操作栏/D 卡牌渲染/E 弹窗/F 信息结算/G 可见性/H 动画 + 三大用户点名系统 DET 右键武将详情/GEN 选将界面/TMR 计时系统),逐桶对照我方 React 产出结构化不一致清单,主控**逐条核实关键项**后合并成 AUDIT_6.5.md(每条带 QML 源 file:line + 我方现状 + 严重度 P0/P1/P2 + 修复建议,共~120 条)。**三大点名系统结论**:计时系统(主计时条/per-Photo 进度条/超时自动结束)整体缺失,vmStore 未消费 MoveFocus;右键武将详情完全缺失(QML 右键 Photo→PlayerDetail→GeneralDetailPage);选将界面只是纯文字按钮,无立绘/势力框/血珠/结果区/VM 规则校验。**复核修正 2 处 agent 误报**(右键经 PlayerDetail 中转非直开 GeneralDetailPage;「主公选将」无独立客户端 UI,是服务端 rule_type/extra_data 行为)。文档末尾给出修复批次建议(批次1 P0必修→批次5 P2打磨)+ 范围裁剪(Cheat/资料库/皮肤/国战专属/配音/战绩API 延后)。本次纯审计未改生产码。下一步待用户认可清单后进修复阶段。

- 2026-06-06 **延时锦囊图标坐标返工(真因:与 HP 完全重叠)**(freekill-web)。上一次"修复"是猜坐标(`left:22→2`)未读全源码,结果延时锦囊图标(乐不思蜀等)与 HpBar **完全重叠**——截图里 HP 血珠下方那个红点就是叠在后面的图标。这次严格读 `Photo.qml`(131×174=PhotoBase 尺寸,与我方 PHOTO_HEIGHT≈174.75 近 1:1):HpBar `x:6,bottomMargin:27`;DelayedTrickArea `x:0,bottomMargin:8`(声明在 HpBar 之后→画在其上,是最低元素,与最低血珠部分重叠)。两者差 19px 是 FreeKill 真实行为。照搬其绝对 bottom、整体上移 20px 避开我方名条(QML 名字在顶部,我方在底部,既有偏差):trick `8→bottom:28 z:6`、hp `27→bottom:47 z:4`,保留 19px 间隔与图标盖血珠的 z 序。web 48 绿、build 干净。**教训补记 memory port-qml-roomlogic-verbatim**:坐标必须读全 QML 锚定+声明顺序(z 序),不能只挪一个数值。

- 2026-06-06 **切片 6 卡面三 bug 修复(手动验收发现)**(freekill-web)。用户截图+元素视图查出三问题,逐一定位真因后修:①**闪等基础牌不显图**:DOM 显示该卡渲染了文字回退(无 `<img>`),但 VM 探针证实 jink→`extension=standard_cards`、jink.png 存在且 200——根因是 `CardFaceView` 的 `artFailed` useState **闩锁后不复位**(卡节点按 cid 长驻,face 异步晚到,早期空 src 触发一次 onError 后永久卡在文字面)。改为照搬 QML `SkinBank.getCardPicture` 的候选链:`cardPicCandidates`(先卡牌 extension,再扫 standard/standard_cards/maneuvering),onError 推进 index、候选集变化时 useEffect 复位 → 既修闩锁又覆盖 extension 缺失/错配。②**观星 popup 卡用数字渲染**:`ArrangeBox` 把卡画成纯 `{cid}` 文本,改用 `CardFaceView`(faces 已在 vmStore 为 arrangeCards 预取)。③**延时锦囊图标坐标错位**(本条已被上方返工条目修正):web 48 绿(+1 cardPicCandidates 测试)、build/tsc 干净。

- 2026-06-06 **切片 6 第六步:R-PERF/R-VM 真机复测(内存诊断)**(freekill-web 提交 7ee0965)。VM 调试面板加"测量内存"按钮(diag/memStats.ts):优先 measureUserAgentSpecificMemory(需 crossOriginIsolated)、回退 performance.memory(Chrome usedJSHeapSize),并统计 /fk 下已加载图片数/体量(resource-timing)。基线:同步美术 215 图~2MB,单局懒加载仅可见立绘+在场卡(1-2MB),VM WASM 堆(数十 MB,Node 已测降级低)为主、美术可忽略,**R-PERF/R-VM 维持低**。最终数待真机点按读取。切片 6 收尾,下一步切片 6.5 全面审计。web 47 绿。

- 2026-06-06 **选牌弹窗遵守可见性 + 规划切片 6.5 审计**(freekill-web 提交 4caad59)。顺手牵羊/过河拆桥等选牌时目标手牌应隐藏(背面盲选)——PlayerCardBox.qml 用 `known=visible_data[cid]!=false`,我的 AskForCardChosen/CardsChosen/Poxi 没读 visible_data。parseGroups 读 visible_data 设每牌 known,RequestPopup 用 faceUp={c.known} 隐藏牌显背面。+1 测试(web 47)。**规划切片 6.5:全功能/UI 对照源码系统审计**(用户提议、认可),写入 M2_detailed_plan.md——逐元素委托 audit agent 对照、可见性专项、按严重度修,制度化收口高保真(根因见 memory port-qml-roomlogic-verbatim)。

- 2026-06-06 **切片 6 第五步:卡面接真实卡图**(freekill-web 提交 18226fa)。CardFaceView 渲染完整卡图(cardPic,按 extension 取包)+ 花色/点数图片叠加(照搬 CardItem.qml/PokerCard.qml:花色 x:3 y:19、点数 0,0,按 width/93 缩放),牌背 card-back.png;缺图回退文字卡面/FK。手牌/桌牌/弹窗统一受益。sync 补 sourcecode/image/card chrome(suit/number/card-back/unknown)→ /fk/image/card。215 图 2.00MB。dev 实测卡图/花色/点数/牌背 200。web 46/lua-native 8 绿。剩第六步 R-PERF/R-VM 真机复测。

- 2026-06-06 **切片 6 全面审计修复 + 1:1 还原失败根因总结**(freekill-web 提交 b2c309f)。委托 audit agent 对照 QML 逐元素查出 ~20 项不一致,修高优先:①**图标退化成文字根因**=readCards 漏读 extension(且误用武将 extension)→ equipIcon/delayedTrickPic 需卡牌包名;补 extension+subtype,对真 VM 验证 qinggang→{standard_cards,weapon}。②装备区改固定 5 槽位(宝/武/防/±马,按 subtype 归位,马用 horse 图)。③判定区用卡牌 extension+尺寸 28×33。④HP useText 三条件 + 文本色按 hp 档。⑤武将名/HP/装备/判定/drawPile 坐标对齐 QML。**根因总结进 memory port-qml-roomlogic-verbatim**:别用调研表代替读源码、数据依赖追到底(SkinBank 用了哪些字段)、固定结构非"能跑就行"、自己vs他人 visible 条件必查、0.75 缩放。他人手牌只显数(按用户要求)。web 46/lua-native 8 绿。

- 2026-06-06 **切片 6 第三步修正:Photo 保真问题(对照 QML 核实 4 项)**(freekill-web 提交 4c2314a)。验收发现:①"FK 蓝方块"=对手手牌被错误渲染成背面浮动卡——QML 从不渲染他人手牌为卡(只徽章+HandcardViewer 文本),CardLayer 现跳过非自己的 hand:<id>;②血珠从下往上扣错了——QML HpBar 是 Column 从上往下、最下珠先空,改 column-reverse→column;③武将名位置错——QML PhotoBase generalName 在 Photo 根 x:5,y:21 竖排=Lua.tr(general),移到 Photo 根竖排标签(双将"主/副"),立绘改纯图;④技能按钮压住自己 Photo——dashboard 技能列左移(right:195)避开。web 46/lua-native 8 绿。立绘/血珠/身份/装备真图已显示。

- 2026-06-06 **切片 6 第三步:Photo 高保真重写(真美术,分层)**(freekill-web 提交 2cf540c)。Photo 从色块+文字改为照搬 Photo.qml/PhotoBase 的分层组件:势力底框+武将立绘(单将满宽/双将 50-50 分屏,缺图回退色块+名)+ HpBar(血珠 magatama 图标非血条,空/1/2/3+ 档,护甲叠加)+ 身份图(role_shown 控制)+ 装备区(EquipArea 图标+名+花色+点数行)+ 判定区(JudgeArea 延时锦囊图)+ 连环 chain + 名/座位条 + 手牌数 + 死亡灰化(deathPic+grayscale)。全美术经 skin.ts,数据经第二步 VM 镜像。装备/判定牌现在渲染在 Photo 内(CardLayer 已跳过这些区,切片5b)。web 46/lua-native 8 绿。**待真机验收**。

- 2026-06-06 **切片 6 第二步:VM 桥读装备/判定/护甲/标记 + 武将 extension**(freekill-web 提交 6381cc9)。readPlayers 扩展返回 shield/chained/dying/role_shown/faceup/sealedSlots/equipCids(getCardIds e)/judgeCids(getCardIds j)/handcardNum/@-标记;新增 readGenerals(name→{extension,kingdom})拼立绘路径。gameStore/VmPlayerLike/syncPlayers 接新字段;cardFaceStore 缓存 general info;vmStore.feed 补取装备/判定卡面 + 武将 extension。对真 VM 验证:青钢剑入装备+乐不思蜀入判定→shield=1/chained=true/equipCids/judgeCids 各 1;readGenerals→caocao{standard,wei}。lua-native 8/web 46 绿。下一步:Photo 分层重写(有视觉变化,需验收)。

- 2026-06-06 **切片 6 第一步:资源 sync + skin 路径解析**(freekill-web 提交 549ff68)。核实资源全貌:武将立绘/卡图/装备图标在各包 `packages/<包>/image/{generals,card}/`,血珠/身份/势力底框等 chrome 在 `sourcecode/image/photo/`,包名(extension)由 GetGeneralData/GetCardData 给。sync-fk-assets 扩展拷两套资源(145 文件 1.80MB,gitignore,懒加载)。skin.ts 照搬 SkinBank 路径构造(generalPic/cardPic/equipIcon/rolePic/magatama...,缺 extension 回退占位)。8 路径单测(web 46)。dev server 实测三类资源 200。下一步:VM 桥扩展 readPlayers 字段(shield/装备cid/判定cid)+ readGenerals。

- 2026-06-06 **M2 切片 5:真实卡面 + dashboard + 战报 + 胜负弹窗 + i18n**(freekill-web 提交 97a8e0f)。严格按工作流:读 Dashboard.qml/GetCardData/Translate/GameOverBox 核实数据来源,先验 VM 读取桥再搭 UI。
  - VM 读取桥(clientVm 预编译句柄,沿用防泄漏):readCards(cids→{name,number,suit,color})、translate(keys→Fk:translate)、readSkills()。对真 VM 验证 slash→杀/♠、caocao→曹操。
  - cardFaceStore(cid→face 缓存,懒取)+ 花色符号/A-K 显示;CardFaceView 渲染文字卡面。Dashboard(技能按钮+prompt+确定/取消/结束,替代 InteractionBar)。GameLogPanel(VM GameLog HTML 行,sanitize,可折叠)。GameOverModal(winner role 判胜负+返回大厅)。
  - i18n:zh.ts 加运行时缓存(registerTranslations,VM 填充);tr() 查缓存→静态→key。武将名/牌名/弹窗将名全本地化。
  - 测试:cardFace helper + i18n 缓存(web 42)、VM 桥(lua-native 7)。全 workspace 绿。卡牌真图/立绘留切片 6。

- 2026-06-06 **完整对局验收通过 + 修桌面牌不清除**(freekill-web 提交 1540f95)。浏览器跑通整局(选将→发牌→出牌→响应→阵亡)无崩溃,核心架构全实证。验收发现的唯一真 bug:桌面出过的牌堆积不清——VM 发 DestroyTableCard(by cid)/DestroyTableCardByEvent(by holding_event_id 阈值)清桌,cardStore 没消费。照搬 RoomLogic.js:548-566 + TablePile vanishTimer:cardStore 记 eventIds per 桌牌,加 destroyTableCards/destroyTableCardsByEvent,vmStore 路由两命令。+3 测试(web 36)。"对方阵亡消失"非 bug(dead 淡化+阵亡遮罩正确,只因无立绘显突兀)。规划剩余切片 5-8 写入 M2_detailed_plan.md。

- 2026-06-06 **修 VM 进游戏崩溃:lua.doString 泄漏 WASM 堆**(freekill-web 提交 73c6c82)。视觉验收选将后秒崩(满屏 readPlayers memory access out of bounds + feedPacket threw Infinity)。捕获真实一局回放进 Node VM 隔离定位:**`lua.doString` 每次编译新 chunk、泄漏 Lua 堆,约 44 次调用后损坏**(_ENV 变 nil,wasmoon 抛栈顶值常为 Infinity)。牌桌每个 packet feedPacket+readPlayers 各一次 doString,第一回合内撞 44 次崩。修:clientVm 把 __fkFeed/__fkReadPlayers/__fkUpdateUI 在 boot 后定义为 Lua 全局 ONCE,之后只调 `lua.global.get` 句柄,不再 doString。实测句柄 2000 次无泄漏、doString 44 次必崩。lua-native +1 回归测试。memory 更新(wasmoon-vite #5)。全 workspace 绿(web 33/lua-native 6/protocol 20)。

- 2026-06-06 **修开局退化:CreateRoom 缺 _game 设置 + AskForGeneral 被 BigInt 丢包**(freekill-web 提交 35a6477)。视觉验收开局秒结束(StartGame→GameOver 无选将)。写 probe 复现确切场景定位两 bug:①CreateRoom 漏发 `_game`/`_mode` 设置块 → 服务端 generalNum=nil → 无法选将 → 游戏瞬间退化结束。补 freekill-core lunarltk/init.lua 默认值(generalNum 3 等)。②补①后 AskForGeneral 发出但网关 `JSON.stringify` 抛 BigInt 错丢包——请求外层 timestamp(毫秒)解码成 BigInt,packetToEnvelope 未归一化 requestId/timeout/timestamp(A9 真实路径咬人)。修:强制转 Number,+1 protocol 回归测试(20 绿)。probe 实测 AskForGeneral(REQ#1)送达。**运维坑**:asio 对中途退出运行中游戏的 IP 临时封禁 20 分钟(网关与 asio 同机共用 IP,一封全连不上);真 asio 进程名是 `./freekill-asio` 非 `build/freekill-asio`,pkill 要匹配对;清封禁须真杀进程重启。

- 2026-06-06 **切片 4b:全面补齐所有请求(对照源码穷举)**(freekill-web 提交 cbb7112)。严格按工作流:穷举 RoomLogic.js ~60 callbacks,核实路径分类(数 request_type 目录:ui_emu 仅 5 个,其余全弹窗),记入 memory。
  - **修 AskForSkillInvoke**:实为 ui_emu(ReqInvoke=OKScene)非弹窗 → 从 popupStore 移除 yes/no,OK/Cancel 走 InteractionBar,prompt 注入 interactionStore。
  - **点选类全做**:AskForChoice/Choices(单/多选项)、AskForCardChosen/CardsChosen(单/多选牌,分组)、AskForGeneral;AG 系列(FillAG/AskForAG/TakeAG/CloseAG)。
  - **拖拽类降级版**(点选不拖拽,满足 reply 结构):AskForGuanxing/Exchange/ArrangeCards→arrange(点牌再点区域,reply [[cids]]);AskForPoxi→降级为选牌。GetPlayerHandcards 自动回手牌;EmptyRequest no-op。MoveCardInBoard/CustomDialog/MiniGame 暂跳过(稀有/扩展)。
  - reply 格式逐个对照 RoomLogic.js 核实(Choice 回值非索引、CardsChosen 回[cids]、Guanxing getResult=[[cids]])。web 33 测试。全 workspace 绿。真拖拽留后续打磨。

- 2026-06-06 **弹窗请求:选将/选项/技能发动(解开局卡死)**(freekill-web 提交 dba46bb)。浏览器视觉验收发现开局即卡——每局第一个请求是 `AskForGeneral`(选将),属"弹窗请求"非 ui_emu/UpdateRequestUI 路径,切片4 没覆盖。读 RoomLogic.js+client.lua 确认:这类(AskForGeneral/AskForChoice/AskForSkillInvoke)client 无 callback→VM 原样 notifyUI 转发,玩家选完直接 replyToServer(request kind,网关盖 requestId)。新增 popupStore + RequestPopup(选将网格/选项/yes-no)。自验:对真实捕获包,VM 发 notifyUI("AskForGeneral",[generals[6],1,...]) generals=["sunquan","caocao","liubei"...],正是 popupStore 消费形状。web 26 测试绿。

- 2026-06-06 **开局流程:等待房间 + 准备/开始**(freekill-web 提交 a380908)。严格按工作流:先读 WaitingRoom.qml + asio room.cpp 核实(记入 memory `waiting-room-ready-start`),再实现。
  - 进房→等待房间(座位 + Ready/AddRobot/StartGame/Quit,可见/可用照搬 WaitingRoom.qml)→ StartGame notify 切牌桌。命令走网关 notify(非 VM 交互);RoomOwner/ReadyChanged/AddPlayer 经 VM 镜像。
  - clientVm.readPlayers + gameStore 补 ready/owner;gameStore 加 capacity(从 EnterRoom)+ started。`waitingState.ts` 纯函数 deriveWaitingState(4 单测)。
  - **自验:start-game.mjs 对真 asio——两客户端建房/满员/准备/开始→双方收 StartGame,游戏跑到 GameOver(PropertyUpdate/ArrangeSeats/GameLog 全在流里)。** 一局真打通。全 workspace 绿(web 22/lua-native 5/protocol 19)。

- 2026-06-06 **M2 切片 4:出牌交互闭环(ui_emu UpdateRequestUI)**(freekill-web 提交 e57724f)。严格按工作流:先深读 ui_emu/*+active_skill+play_card+client.cpp 核实交互/reply 机制(记入 memory `ui-emu-request-architecture`),再实现。
  - interactionStore 消费 UpdateRequestUI change diff;clientVm.updateRequestUI 驱动 VM 全局函数(本地循环非服务器往返);InteractionBar(prompt+OK/Cancel/End);CardLayer 手牌可点+选中升起 20px(照搬 ItemArea);Photo 目标高亮(绿可选/红已选)。
  - 网关:asio-client 跟踪最近 request 的 requestId(镜像 QML router this->requestId),ws-bridge 给 reply envelope 盖该 id(asio expectedReplyIds 匹配)。VM 的 ReplyToServer notifyUI → 网关 reply。
  - 自验:lua-native feed.test 实测真 VM 全闭环——PlayCard→选杀→VM 算出合法目标→OK 亮→ReplyToServer 发 {card:slashId, targets:[2]}。web interactionStore 6 单测。全 workspace 绿(protocol 19/lua-native 5/web 18)。**牌桌可玩。**

- 2026-06-06 **M2 切片 3:卡牌位置 + 飞行动画层(R-ANIM 头号难点)**(freekill-web 提交 2940c7e)。严格按工作流:先 3 个并行 Explore 深度调研 + 逐条核实(揪出 ui_emu 真实位置、确认 MoveCards 的 VM 输出 merged[] 形状≠原始 asio 包),写 `分析/M2_detailed_plan.md`(M2 分 5 切片详规划),再实现。
  - cardStore:区域模型(drawPile/tablePile/hand:ID/equip:ID/judge:ID)+ applyMoveCards 照搬 RoomLogic moveCards/getAreaItem(区域映射、Void 离场、known 正背面)。
  - CardLayer:全卡绝对定位浮层(脱离 React reconciliation 防瞬移)+ WAAPI 500ms OutQuad(照搬 goBack);areas.ts 几何照搬 Room.qml(tablePile/drawPile)。
  - 自验:lua-native feed.test 断言真 VM 输出 MoveCards 的 merged[](ids/fromArea/toArea)= cardStore 消费契约;cardStore 5 单测覆盖 draw/play/discard/void/equip。全 workspace 绿(protocol 19/lua-native 4/web 13)。
  - 卡面占位(cid 数字/FK 背),真 SkinBank 图留切片 5。新增 memory `ui-emu-request-architecture`。

- 2026-06-06 **清理实现审计待办 A1-A12**(freekill-web 提交 88d8af6)。按优先级修复并验证 A1-A4/A6-A9/A11-A12,A5/A10 依赖未建里程碑延后(详见计划 §11)。亮点:**A11 补 COMPRESSED 测试时揪出潜伏网关 bug**——qzlib 的 `(0,eval)('require')` 在 ESM 下 undefined、遇压缩包运行时抛错,改 `process.getBuiltinModule('node:zlib')`(同步/无 import/浏览器 guard)。A2 逐行照搬 arrangeManyPhotos(>8人含缩放)。A8 握手判定收紧(须见已知 OK 命令+缓冲重放)。全 workspace 绿(protocol 19/lua-native 4/web 8),gateway 握手+m1-e2e 对真 asio 通过。**A5(网关 md5,阻塞于 assets 生成器)、A10(i18n,阻塞于 M3 Fk:translate)已记录延后原因。**

- 2026-06-06 **用「实现纪律」倒查全部已写代码(实现审计)**。4 个 Explore 并行审计 protocol/lua-native/web/gateway + 人工逐条核实(审计员有误报,如 getMicroSecond 被指差 1000 倍——核实 GetMicroSecond 真返微秒、`Date.now()*1000` 正确)。核实属实的待修项记入实现计划 §11「实现审计待办」(A1-A12,按高/中/低)。高危 3 项:getDisabledPacks 桩(A1)、座位>8人未实现(A2)、错误静默吞掉(A3)。无立即阻断当前基础对局开发的项(高危项都在"接更多包/更多人/排查时"才触发)。**未在本次修复**——先登记,按里程碑优先级处理。

- 2026-06-06 **修复迟入房竞态 + 确认自身布局符合 QML**(freekill-web 提交 f4666e3、66a4625)。
  - **名字 Tester 已修**(f4666e3,见下条):大厅暂存登录 Setup,VM 引导后回放 → 浏览器实测 webtester/yueying 名字正确。
  - **迟入房看不到已有玩家(竞态)**:EnterRoom 启动异步 boot 后立即返回,boot 期间到达的 AddPlayer 见 inRoom=true 就 feed,但 vm 仍 null → 被丢弃。修:把 boot 链到 feedChain,后续所有 feed(含 boot 期间到达的)排队等 boot 完成,Setup→EnterRoom→房间包按序不丢。`scripts/late-joiner.mjs` 对真网关自验:A 建房、B 迟入,断言 B 收到 A 的 AddPlayer(通过)。
  - **确认非 bug**:自己 photo 在右下角符合 QML(Explore 读 Room.qml 证实:自己 photo 在 photos Repeater 里、index 0 = regions[0] 右下;底部满宽是单独的 dashboard 手牌区,尚未实现)。
  - **副发现**:AddPlayer 内层 name/avatar 是 CBOR 字节串、解码成 `{0:..,1:..}` 字节对象而非字符串 → 印证花名册必须读 VM 镜像(getScreenName),不能信 AddPlayer 原始增量。
- 2026-06-06 **修复座位/自身显示 + 沉淀实现纪律**(freekill-web 提交 6ad13d6、f4666e3)。M2 第二切验收暴露多个 bug,根因是方法论问题(自己发明代替照搬、没先读透源码/计划、桩当生产代码):
  - **座位错位**:自写 `toRelativeSeat` 致等待房间全员叠一格 → 改为逐行照搬 RoomLogic.js:`seatPosition(index)` = `regions[seatIndex[index]]`,index 由 ArrangeSeats 旋转(Self 置 0)/等待房间按 VM 顺序。
  - **名字显示 Tester/张冠李戴**:①prelude CppClient 写死 self=`makePlayer(1,"Tester")`(spike 桩),靠登录 Setup 覆盖;②但登录 Setup 在大厅阶段到达、VM 尚未引导(VM 在 EnterRoom 才建)→ Setup 没喂进 VM → self 停在桩。修:大厅暂存登录 Setup,VM 引导后先回放 Setup 再喂 EnterRoom。Node 实测 Setup[7,"webtester"]→selfId 7。
  - **花名册双写**:apply 的 AddPlayer 与 syncPlayers 双写致冲突 → 花名册只由 syncPlayers 读 VM 镜像,apply 只管 selfId/marks/started。
  - **沉淀工作流**:CLAUDE.md 新增「实现纪律」节(先读后写、能照搬就照搬、VM 是真相源、spike 桩≠生产、自验在前别拿用户当 CI);新增 memory `port-qml-roomlogic-verbatim`。
- 2026-06-06 **M2 第二切:fixed-stage 玩家座位 + VM 出站回路**(freekill-web 提交 ddd45be)。
  - **座位渲染**:`table/seatLayout.ts` 逐行移植 RoomLogic.js arrangePhotos(1200×540 逻辑舞台);Stage(viewport scale=min(vw/1200,vh/540))+ Photo(武将/HP/身份/势力/座位,占位美术)+ RoomScene(相对自己落座)。`gameStore` 消费数组形 notifyUI 增量(AddPlayer[id,name,avatar,ready,time]/RemovePlayer[id]/ArrangeSeats[ids]/PropertyUpdate[id,prop,value]/SetPlayerMark[id,mark,value]/StartGame),字段形状从 clientbase.lua 核对。
  - **VM 出站回路**:`createNatives` 分出 `onNotifyServer`(VM→服务器,如 Heartbeat),vmStore 转 `gatewayClient.notify`(serverSender 由 connectionStore 注入避免循环依赖)→ 网关 → asio。面板不再显示 `__toServer__:`。分工见 [[vm-outbound-vs-react-reply]]。
  - **测试**:web `table.test.ts` 6 个(seatLayout 坐标 + gameStore reducer);全 workspace build/test/typecheck 绿。
  - **验收准备**:asio 配置启用 AddRobot(原 disabledFeatures 含它),便于单人加 bot 看满座牌桌。
- 2026-06-06 **M2 第一切:客户端 VM 在浏览器跑起来**(freekill-web 提交 11ca327)。首次让 freekill-core 客户端 Lua 在真实浏览器环境运行。
  - **wasmoon + Vite 集成解决**:`LuaFactory(glueWasmUrl)`,glue.wasm 经 Vite `?url` 作 hash 资源服务;vite build 正确产出 glue.wasm(271KB)。
  - **资源送达**:`apps/web/scripts/sync-fk-assets.mjs` 拷 freekill-core lua/json + prelude + 生成 file-list.json 到 public/fk(gitignore,从上游再生);`lua-native` 加 `mountFromFetch`(浏览器 fetch 挂载)。
  - **协议加 raw 字段**:envelope 带原始 inner CBOR 的 base64(VM 的 ClientCallback 要原始 CBOR,非 JSON——cbor-x 重编码非字节等价于 asio)。`packetToEnvelope` 附带,`base64ToBytes` 浏览器侧还原。
  - **qzlib 浏览器可打包**:node:zlib 改间接 require 懒加载(Qt-zlib 仅网关用),否则 protocol 包拖 node:zlib 进浏览器 bundle 编译失败。
  - **VM 集成**:clientVm.ts(建 engine/挂资源/bootClient/feedPacket)+ vmStore(生命周期 + notifyUI 计数)+ VmDebugPanel;envelope router 在 EnterRoom 引导 VM 并按序喂入所有房间 packet。
  - **验证**:lua-native `feed.test.ts` 引导 VM + 喂真实捕获 packet 流,断言 notifyUI 含 MoveCards(浏览器喂包路径的回归测试);全 workspace build/test/typecheck 绿。**浏览器运行时待人工确认**(Node 路径已绿、vite build 已产 wasm)。
- 2026-06-06 **M1 大厅完成**(freekill-web 提交 fa47e8f)。`apps/web`(React+Vite+Zustand):net/gatewayClient(WS 封装,发 `__gateway_login` 控制消息,envelope 路由)、stores(connection/auth/lobby,envelope router 映射 UpdateRoomList/UpdatePlayerNum/Chat/EnterRoom)、LoginPage/LobbyPage、RoomList/ChatBox/CreateRoomDialog、i18n/zh(gameMode 静态词典)。`apps/gateway` 改:接受浏览器经 `__gateway_login` 传入的凭据(宽限期后回退 config),AsioClient 支持 per-connection Credentials,不日志化密码。
  - **大厅不集成 wasmoon**:读 asio lobby.cpp + QML Lobby.qml 确认大厅纯结构化 packet,QML 里 `Lua.` 几乎全是 `Lua.tr` 翻译,VM 推迟到 M2 牌桌。见 [[lobby-needs-no-vm]]。
  - **命令格式核对自 asio 源码**:CreateRoom `[name,capacity,timeout,settings]`、EnterRoom/ObserveRoom `[roomId,password]`、UpdateRoomList 项 `[id,name,gameMode,playerCount,capacity,hasPassword,outdated]`、Chat `{type:1,msg}`、UpdatePlayerNum `[在线,总]`。
  - **E2E 验证**:`scripts/m1-e2e.mjs` 对真 asio 跑通 `__gateway_login → 登录 → EnterLobby → CreateRoom → EnterRoom`。build/test/typecheck 全绿(7 包)。手动:gateway(9528)+ vite dev(5173)起,浏览器登录可见大厅。
- 2026-06-06 **M0 网关连通实现 + 端到端验证**(freekill-web 提交 c94af38)。`apps/gateway`:config(env/CLI,ASIO_HOST 必填——WSL NAT IP 每次重启会变)、rsa(Setup 密码 = RSA-PKCS1(32字节非零前缀+密码))、asio-client(TCP+PacketStreamDecoder,握手 NetworkDelayTest→Setup→lobby)、ws-bridge(WSS,每浏览器 1:1 asio 连接,envelope 双向,zod 校验)、index 入口。`packages/protocol` 补 convert(packet↔envelope、buildSetupPacket、extractPublicKeyPem,内层 data 解码归一 BigInt/Map 为 JSON)。
  - **实测**:本地 Ubuntu 26.04 WSL 构建运行 asio v0.1.14,Windows Node 经网关 WSS 登录 → 自动注册 → 收到 `Setup→SetServerSettings→EnterLobby→UpdatePlayerNum`。m0-smoke.mjs(端到端)+ handshake.test.ts(集成,无 ASIO_HOST 时 skip)通过。
  - **两个握手 bug 实测发现并修复**:①cbor-x 默认给 Uint8Array 打 tag 64(0xd840),asio 只认裸字节串 → 必须 `tagUint8Array:false`,否则 "INVALID SETUP STRING";②Setup 密码的 32 字节占位前缀不能含 NUL(asio 用 `std::string{buf}` 按 C 串截断)→ 用非零随机字节,否则 "unknown password error"。记入 [[cbor-x-asio-gotchas]]。
  - **环境**:WSL2 默认 NAT,Windows 连 asio 须用 eth0 IP(非 127.0.0.1);asio 构建/常驻运行手册见 [[asio-wsl-runtime]];MD5 握手机制 + 权威值见 [[asio-md5-handshake]](网关当前硬编码 FK_MD5,待 assets 生成器替代)。
- 2026-06-06 **freekill-web/ pnpm workspace 骨架落地**(独立 git 仓库,GPLv3,初始提交 e539d32)。`pnpm install/build/test/typecheck` 全绿(6 包)。
  - **写实包**:`packages/lua-native`(沉淀 spike 的 fkprelude.lua/server_fkprelude.lua + natives.ts〔去 node:path 同构〕+ boot.ts;单测挂载真实 freekill-core 建出 ClientInstance = Gate1-2 转正式回归测试);`packages/protocol`(codec.ts:裸帧增量解码用自写 CBOR item-length 扫描器〔cbor-x 不暴露消费字节偏移〕、command/data 字节串 0x40、Qt-zlib;types/envelope/schema;单测对 2708 真实捕获 payload 解码 + 自有 framing round-trip)。
  - **占位桩**:apps/web(Vite+React 最小挂载)、apps/gateway(protocol 接线 + M0 TODO)、packages/shared、packages/assets(manifest 生成桩,asio flist/MD5 算法已注释固化)。
  - **关键技术坑**:①cbor-x 默认 `mapsAsObjects:true` 解 asio payload 抛 "Invalid property name type object"(字节串不能当 JS 对象 key),须 `mapsAsObjects:false`;②cbor-x 重编码**非**字节级等价于 asio 编码器(map 序/整数尺寸不同,500 包仅 51 一致)→ 不能用"重编码字节相等"做测试断言,也不能靠重编码测消费字节;③wasmoon emscripten FS 无 `FS.isDir`(已在 natives.ts 用 mode 位修复)。
  - **文档一致性修复**:freekill-web-spike/README.md 之前指向不存在的 `wasmoon_client_lua_spike_report.md`,改指实际主文档 freekill_web_implementation_plan.md(§13 证据)+ PROGRESS.md,并补全 perf/vm/native-audit 脚本说明。
- 2026-06-06 **R-NATIVE 客户端原生面审计完成**(`native_audit.mjs`,`native-audit.json`)。三方交叉验证:
  - **运行时陷阱法**:给 `fk` 装 `__index` 陷阱,全量 30 包(3295 武将/1482 牌/12947 技能)加载 + 建 ClientInstance,记录所有被读为 nil 的 `fk.*`。结果:**唯一缺口 `fk.CreateTriggerSkill`**(仅 joym 调用)。
  - **SWIG 清单对照**:freekill.i/client.i/qt.i/player.i 的客户端原生面逐项对照 prelude——全覆盖。`fk.QJsonDocument`/`fk.QRandomGenerator`/`addQmlImportPath` 客户端运行时零引用(仅 lsp 类型注解);RNG 实走纯 Lua `fk.rand=require"xoshiro256starstar"`。
  - **clientbase 运行时调用对照**:只用 9 个 CppClient 方法(addPlayer/getSelf/notifyServer/notifyUI/removePlayer/saveGameData/saveRecord/sendSetupPacket/setupServerLag),prelude 全提供。
  - **结论**:客户端原生面**已完整**。`fk.CreateTriggerSkill` 是 joym 旧包直接调 new-core 已删除的 API(new-core 只留 `fk.CreateCard/CreateGameMode/CreateSkill`),属包与 new-core 自身不兼容,非 Web 缺面。`saveRecord`/`saveGameData` 当前 no-op,沙箱下录像/存档需接 IndexedDB/OPFS 或网关(§13 已记的 io 适配项,非原生面缺失)。R-NATIVE 由"中"**降级为低**。
- 2026-06-06 **R-VM 长稳验证完成**(`vm_spike.mjs`/`vm_run.mjs`,`npm run vm`,`vm-result.json`)。回放真实一局 packet 流,fresh / reuse 两模式各连打 50 局(reuse 另测 100 局):
  - **external(WASM 线性堆)零增长**:fresh 19.4→35.4MB(第 2 局起平台化,slope 0.038MB/g);reuse 恒定 19.4MB(slope 0,100 局如一日)。→ **VM 无泄漏**。
  - RSS 单调爬升(fresh 67→199MB、reuse 67→125→100 局 150MB)纯属分配器碎片:reuse 100 局 RSS 前半 1.198MB/g、后半塌到 0.065MB/g(平台化),external 却纹丝不动 → 证明非泄漏。
  - **结论**:计划 R-VM 解①(每局新 VM、离开即 close 重建)成立且安全;**reuse 模式更省**(external 19.4 vs 35.4MB,RSS 峰值 125 vs 200MB),可作备选降峰值。RSS 碎片真浏览器复核列入待办。R-VM 由"中高"**降级为低**。
- 2026-06-06 **R-PERF 度量完成**(`perf_spike.mjs`/`perf_run.mjs`,Node v24.11.1,`perf-result.json`)。三场景 Node 实测:
  - base(4 包):297 文件/1.59MB,挂载 106ms,启动 60ms,RSS Δ6MB(51→58MB)。
  - selective(8 包,base+utility/sp/tenyear/ol):2599 文件/7.94MB,挂载 960ms,启动 367ms,RSS Δ33MB(66→98MB)。
  - full(30 包):7641 文件/21.19MB,挂载 2.9s,启动 985ms,RSS Δ86MB(91→177MB)。引擎:3295 武将/1482 牌/12947 技能/40 模式。
  - **结论**:全量在 Node/wasmoon 下可加载,峰值 RSS ~177MB、启动 <1s,数量级未失控 → 验证选择性加载即可把成本压到 base/selective 区间。挂载耗时(FS 往返)是主成本,对应 R-PERF 解③(bundle/预编译)。R-PERF 由"中高/阻塞"**降级**;真浏览器复测列入待办。
- 2026-06-06 **修复 isDir 原生面 bug**(`fknatives.mjs`):wasmoon 的 emscripten FS **不暴露 `FS.isDir()`**,原 spike 的 `emfs.isDir(...)` 必抛错返回 false,致 `ModManager:loadPackages` 发现循环对每个扩展包 `FileIO.isDir` 判否、**静默只加载 4 基础包**。改为直接测 mode 位 `(mode & 0o170000) === 0o040000`。base spike 因基础包走显式 require 未触发此路径,故此前未暴露。
- 2026-06-06 **R-NATIVE 已知缺口**:`joym` 包用旧语法 `fk.CreateTriggerSkill{...}`(legacy `compat.trigger_legacy`,新 core 中已注释禁用),30/31 包加载时唯一 qCritical。属**旧包与新 core 自身不兼容**,非 Web 端缺面;记入 R-NATIVE 待办,不阻塞。
- 2026-06-03 建立自动化工作流:`.claude/scripts/project-state.mjs`(确定性状态生成器)、SessionStart 钩子(`.claude/settings.json`)、`/sync` 命令(`.claude/skills/project-sync`)、`CLAUDE.md`(工作指南)。
- 2026-06-03 三份分析文档整合为单一 `freekill_web_implementation_plan.md`。
- 2026-06-03 完成 wasmoon 可行性 spike(六关全通过)。
